{
  "summary": "Adversarial first-principles review of the G7 watch acquisition approach against external implementations (xDrip, Juggluco, CoreBluetooth); refute our conclusions; no code",
  "agentCount": 9,
  "logs": [],
  "result": {
    "verdict": "# G7-on-watchOS connection problem — honest synthesis\n\n## 1. VERDICT on \"half-poisoned CBCentralManager\": PARTLY RIGHT, WRONG WHERE IT COUNTS (the label and the fix)\n\nThe *phenomenon* you named is real and documented: a central whose scan/RX path keeps delivering ads during a window while the armed connect never completes. Apple-forum reports of stuck-`.connecting` since iOS 11, the iOS-17 state-restoration variant, and Apple DTS's flat statement that \"CoreBluetooth on watchOS does not work the same way it does on iOS\" (Source 5, threads 94011 / 742497 / 774914) all match your signature. You are not imagining it.\n\nBut \"the manager object is *poisoned* and *replacing it* is the cure\" is the weakest available explanation of that phenomenon, for three reasons:\n\n- **It requires an undocumented mechanism** — a manager whose RX path lives while its initiator path selectively dies. No source found a primary citation for that as a property of the manager object. Meanwhile a fully-documented chain produces identical symptoms: watchOS caps BLE at **2 connections per app** (Apple staff, thread 738861); you already hold the Omnipod/OmniBLE link (1 of 2); `cancelPeripheralConnection` is documented **nonblocking and does not guarantee the physical link drops** (Apple docs, verbatim in Source 5), and on iOS 17 sometimes no-ops entirely; a peripheral stuck in `.disconnecting` is not `.disconnected` and almost certainly still consumes the slot. Net: the next armed `connect()` is silently slot-starved, the initiator never fires, and you still hear ads because RX is a separate path. That reproduces *all four* field signatures (first-read-succeeds, next-windows-miss, scan-still-sees-ads, recreate-helps-for-a-while) using only Apple-documented behavior.\n\n- **Your keystone inference is a link-layer category error.** \"The observer sees the connectable ad, so the connect should have fired\" does not hold: scanning is receive-only and never transmits `CONNECT_IND`; initiating is a distinct, time-sliced LL procedure that additionally requires the initiator to be armed, win its RX slot, TX `CONNECT_IND`, and **have the sensor accept it** (Source 5; BLE LL spec; US Patent 9,706,496). Corrected, the observation cuts the *other* way: if the *same* central is still delivering scan callbacks during a miss, that central's RX pipeline is demonstrably alive — evidence the manager is *not* dead.\n\n- **\"Recreate cures it\" is confounded and only ~50% effective.** Deallocating the central simultaneously (a) frees your local slot, (b) hands you a *fresh CBPeripheral instance* — which alone fixes the iOS-17 stale-object bug your own note \"fresh handle=false always\" (`G7Client.swift:1216`) points straight at — and (c) burns wall-clock that can straddle the sensor's own ~15-min slot-reclaim. You have no control arm isolating which. And the forums report a fresh central only *sometimes* works, occasionally coming back `.unknown`; the reliable cures were BT-toggle / force-quit / reboot. A true \"the object was corrupt, swap it\" fix would be near-deterministic. **Your own build that automates the recreate still produced three consecutive misses** — a direct failed prediction of the poisoned-central hypothesis.\n\nMost defensible root cause: **an incomplete BLE teardown on watchOS leaves the G7 link half-open, consuming one of your app's two connection slots (and possibly one of the sensor's three), starving the next connect.** \"Poisoned central\" and \"connection didn't release\" are the same defect seen from two ends (Source 4's synthesis). The mislabel matters because it prescribes the wrong fix.\n\n## 2. VERDICT on connect-per-reading vs persistent: CONNECT-PER-READING IS MANDATORY — but your *implementation* of it is where the bug lives\n\nPlainly, on what the external implementations actually do: **none of them hold a true persistent, notification-driven connection to a G7, because the sensor won't allow one.**\n\n- Loop's own `G7SensorKit/.../G7BluetoothManager.swift:236–250` (Pete Schwamb's comment, verified twice this session): *\"Persistent connections don't seem to work with the transmitter shutoff… The OS won't re-wake the app unless it's scanning.\"* The transmitter powers its radio **off** between readings.\n- Juggluco's author: *\"Dexcom G7/ONE+ disconnects also on phones after every glucose value.\"* Sensor-initiated, every platform, including a shipping Wear OS watch (which uses exactly your model: an exact-alarm wake to connect per window).\n- xDrip4iOS *looks* persistent but is not — its base class treats the periodic drop as `// Expected short-lived disconnect (normal Dexcom behavior)` and re-arms `connect()` every window. And critically it is a **bond-free coexistence reader**: it runs **no J-PAKE at all**, free-riding the Dexcom app's system-wide bond. That regime does not exist for a sole-central watch. Do not treat xDrip's \"persistence\" as a precedent you can copy.\n\nSo the label \"connect-per-reading\" is correct and must stay. The refutable part is the **three implementation choices bundled under it**, and every reference collector diverges from you on them:\n\n1. **You self-issue `cancelPeripheralConnection` after each read.** xDrip+ instead writes an application-layer `DisconnectTxMessage` to the Control characteristic and lets the *sensor* tear down (Source 3, `Ob1G5StateMachine.disconnectNow()`). Loop's own kit inserts `Thread.sleep(2s)` to let the transmitter shut down, then scans. Nobody races the sensor's own teardown with an OS-level cancel the way you do — and that race is the mechanism that produces your `.disconnecting`-stuck peripheral. Your standing justification (\"we must re-handshake because the bonded shortcut left the link unencrypted\") is a non-sequitur here: the re-handshake cost is imposed by the *sensor dropping you*, which happens whether or not you self-cancel. It does not justify the proactive teardown.\n\n2. **You arm a pending connect with no active scan; your own data says the scan path is the one that still works on a miss.** Loop scans-then-connects-on-discovery; xDrip+ escalates to `always_scan` on connect failures (\"Switching to scan always mode due to connect failures metric\"). You are riding the initiator path your field data proves is dead and avoiding the RX path it proves is alive.\n\n3. **You re-run the full J-PAKE + cert + PoP every window.** Juggluco bonds once at the OS level and does only a short AES challenge on subsequent connects (`DexGattCallback.java:132–142`). Full-handshake-every-window holds the link/slot materially longer — aggravating exactly the slot/teardown pressure that is likely your root cause.\n\nImplication, stated flatly: **you cannot escape to a persistent connection — the firmware forecloses it — but you also don't need to. Most of the imagined benefit of \"persistent\" comes from ceasing to fight the mandatory per-window teardown**, not from holding a link.\n\n## 3. The 2–3 things you have most likely gotten WRONG\n\n1. **Attributing the fault to the manager object rather than to connection lifecycle / the watchOS 2-slot budget.** The single most consequential omission in the whole investigation is that you hold the Omnipod link on a platform capped at 2 BLE connections per app. A leaked G7 link puts you at the ceiling. This mechanism was apparently never in your model, yet it explains everything the poisoned-central story does with documented constraints.\n\n2. **Treating \"scan sees the ad\" as proof the connect should have fired.** This link-layer category error is load-bearing for the entire \"scan works, connect is dead, so the central is half-poisoned\" narrative. Corrected, the observation argues the central is alive.\n\n3. **Self-issuing the post-read disconnect and calling it clean.** You are almost certainly racing the sensor's own radio-shutoff with a nonblocking cancel that doesn't guarantee teardown — manufacturing the `.disconnecting`-stuck state you then try to cure with the heavier central-recreate hammer. You are treating a symptom you created.\n\n(Adjacent, lower-confidence: reaching for central-recreate at all. It maps to xDrip+'s *last-resort* 12-failure adapter-restart watchdog, not anyone's first-line fix. No mature implementation in the ecosystem recreates the central per-degradation.)\n\n## 4. Highest-value next step, and the Dexcom binary\n\n**Do NOT pursue the Dexcom binary.** Source 6 is decisive: FairPlay encryption needs a jailbroken device you likely can't source; Swift decompiler output is barely legible; and — fatally — it's the wrong OS (iOS, not watchOS) solving the wrong problem (near-persistent primary collector, not standalone connect-per-reading). CoreBluetooth is a public framework; there is no secret private-API path to find. The protocol is already fully open across xDrip/Juggluco/G7SensorKit. Binary RE has ~zero transferable value plus a real legal shadow.\n\n**Highest-value step is a single experiment that discriminates the two hypotheses with no code change to the connect path — run two things across a natural miss with the central NOT recreated:**\n\n- **Stop self-issuing `cancelPeripheralConnection` on the success path; let the sensor's own post-read disconnect land.** Measure whether the `.disconnecting`-stuck rate and the subsequent miss rate fall. If they do, the self-teardown race is confirmed as the mechanism and \"recreate the central\" is exposed as symptom-treatment. This is the cheapest high-information move you have (Source 4's proposed test).\n- **In parallel, during a blackout, log the inter-advertisement interval via your 131-window observer.** 5-min spacing ⇒ the sensor still believes a client is healthy (your link is half-held → slot/teardown fault, client-side). 1-min \"rapid reconnect\" spacing ⇒ the sensor considers you gone (initiator/peripheral-object fault, slot-occupancy *exonerated*). This one measurement cleanly separates the remaining candidates.\n\nArchitectural direction if the experiment lands as predicted: move to the model already sitting in your own `G7SensorKit` and mirrored by xDrip and Juggluco — **let the sensor initiate teardown (or write the protocol-level DisconnectTxMessage), insert a settle delay, re-acquire by active scan + connect-on-discovery, and manage the 2-slot budget explicitly (adopt via `retrieveConnectedPeripherals` / cleanup-loop stale system connections at session start).** Investigate OS-level bonding to collapse per-window J-PAKE to a short AES challenge as a second-order win.\n\n## 5. What remains genuinely UNKNOWN (needs more data)\n\n- **Whether a `.connecting`/`.disconnecting` peripheral actually counts against the watchOS 2-slot budget.** The 2-per-app cap is Apple-confirmed; whether a wedged (non-`.disconnected`) peripheral consumes a slot is inference, not documented. This is the linchpin of the leading hypothesis and is currently unverified.\n- **Whether the sensor's 3-slot accounting is actually implicated at all, or purely the watchOS client side.** Only the inter-ad-interval probe and a `retrieveConnectedPeripherals` \"is the G7 still system-connected during a miss?\" check can settle it. The ~15-min sensor slot-reclaim timeout is an order of magnitude longer than your 5-min miss cadence, which *argues against* sensor-side slot-hold as the operative cause — but this is not proven.\n- **Whether a standalone, full-handshaking, sole-central watch collector can be made reliable at all.** Honest gap flagged by Source 6: no existing open implementation does connect-per-reading, standalone (own J-PAKE), on watchOS, inside a workout session. xDrip free-rides a bond; Juggluco bonds at the OS level and can `unbond()`/re-pair; Loop's kit rides alongside the official app. Your exact combination is unsolved anywhere in the ecosystem — so some of this you will have to establish empirically rather than borrow.\n- **Whether central-recreate is causal or coincidental.** Cannot be resolved without a control arm that waits the same wall-clock duration *without* recreating and measures recovery rate. Until you run that, the recreate's apparent efficacy is confounded and cannot be trusted as evidence for anything.\n\nKey source anchors: `G7BluetoothManager.swift:236–250` (transmitter-shutoff, scan-after-2s-delay); `G7Client.swift:1043` (proactive cancel), `:1216` (stale-handle fingerprint), `:570–587` (slot selector); Apple thread 738861 (watchOS 2-connection cap); Apple `cancelPeripheralConnection` docs (nonblocking, no teardown guarantee); xDrip+ `Ob1G5StateMachine.disconnectNow()` (protocol-level disconnect); Juggluco `DexGattCallback.java:132–142` (bonded auth-skip).",
    "refutations": [
      "# REFUTATION: \"The CBCentralManager becomes half-poisoned; recreating it is the cure\"\n\nI was asked to break this conclusion, not defend it. Here is the strongest case that it is wrong, built entirely from the evidence in your six sources plus first-principles CoreBluetooth/link-layer semantics.\n\n## The conclusion smuggles in two claims. Both are weak.\n\nYour conclusion is really a conjunction: (1) the *locus* of the fault is the CBCentralManager object (\"poisoned\"), and (2) recreating it is *causally* the cure. To hold, BOTH must be true. I'll attack each, then attack the diagnostic reasoning that led you there.\n\n---\n\n## (a) The slot / incomplete-disconnect alternative explains every observation the \"poisoned central\" story explains — with fewer assumptions and better documentary support\n\nThe poisoned-central story requires a novel, undocumented failure mode: a CBCentralManager whose RX/scan path stays alive while its initiator path selectively dies. No source produced a single primary citation for *that specific split* as a manager-object property. What the sources DID document, from Apple directly, is a chain that produces identical symptoms:\n\n- **watchOS caps BLE at 2 connections per app, Apple-confirmed** (Source 5, forum 738861). Your app already holds the Omnipod (OmniBLE) link. That is 1 of 2. A G7 connection that doesn't fully release sits in the second slot.\n- **`cancelPeripheralConnection` is documented nonblocking and does NOT guarantee link teardown** (Source 5, Apple docs verbatim: \"canceling a local connection does not guarantee that the underlying physical link is immediately disconnected\"). On iOS 17 there are corroborated reports it sometimes produces *no disconnect and no error at all*.\n- **A peripheral not in `.disconnected` almost certainly still consumes a slot** (Source 5, INF but tightly reasoned). Your \">5s stuck in `.disconnecting`\" is precisely a peripheral that is not `.disconnected`.\n- **Result:** next window's armed `connect()` has no free slot → the OS never enters the initiator state → no `didConnect`, no `didFailToConnect`, no error (connect never times out). You still *scan*-hear the sensor because RX is a separate, unaffected path.\n\nThis reproduces all four of your field signatures — first-read-succeeds (clean slots), following-windows-miss (leaked slot), scan-still-sees-ads (RX fine), recreate-cures-for-a-while (recreate frees the local slot until the leak re-accumulates) — **without positing any poisoning of the manager at all.** By parsimony alone, the slot-leak hypothesis dominates: it uses only Apple-documented mechanisms; yours requires an undocumented one.\n\nThe Source-4 synthesis makes the unifying move explicit and it is the strongest single point against you: **\"poisoned central\" and \"connection didn't release\" are the same defect seen from two ends — an incomplete teardown in the watchOS BLE stack.** If that's right, \"the central is poisoned\" isn't a distinct root cause; it's a mislabeling of an incomplete-teardown/slot-budget problem. And the mislabel matters, because it points at the wrong fix (rebuild the manager) instead of the right one (guarantee teardown / manage the 2-slot budget / adopt via `retrieveConnectedPeripherals`).\n\n---\n\n## (b) \"Recreate cures it\" is confounded and may be coincidental with a timeout you're not measuring\n\nThe causal claim rests on temporal correlation: you recreate, the next window catches. But at least three uncontrolled confounds ride along with every recreate:\n\n1. **The recreate is not the only thing that changes.** Deallocating the central drops your app's local connection, releases the old `CBPeripheral` object, and forces a fresh discovery yielding a *new* peripheral instance. Source 5's iOS-17 mechanism (forum 742497) is that CoreBluetooth hands back a peripheral with the *same identifier but stale state*; code that re-arms `connect()` on the stale object connects to a dead handle forever. Your own note — \"fresh handle=false always,\" `G7Client.swift:1216` — is the fingerprint of exactly this. **If that's the mechanism, the cure is \"got a fresh peripheral object,\" and rebuilding the manager is just the sledgehammer that happens to also do that.** The manager was never the poisoned thing; the retained peripheral was.\n\n2. **The recreate takes wall-clock time, which overlaps the sensor's own slot-timeout.** The G7 frees a slot on its own after ~15 min / 3 missed acknowledgments (Source 4, xDrip rapid-reconnect doc). Your recreate + rediscover + next-window arm can straddle a sensor-side timeout. When connectivity returns, you credit the recreate; the sensor may simply have reclaimed the leaked slot on schedule. You have no control arm that says \"wait the same duration WITHOUT recreating\" — so the two explanations are observationally tangled in your current data.\n\n3. **Recreate is reported as only ~50% effective for the truly wedged state** (Source 5, forums 94011/84098: cancel+retry, adv-address toggle, and even a new CBCentralManager only *sometimes* work; the reliable cures were BT toggle / force-quit / reboot, and a fresh central sometimes comes back `.unknown`). A genuine \"the object was poisoned, replace the object\" cure should be near-deterministic. A ~50%, sometimes-worse cure looks far more like nudging a *system-level* condition (reference-counted physical link, radio arbitration, slot budget) that the manager instance doesn't fully own.\n\n---\n\n## (c) \"The observer sees ads, so the connect should have fired\" is a category error — and it's load-bearing for your whole diagnosis\n\nThis inference is the keystone of \"the scan path works but the connect path is dead,\" which is what makes the fault sound like a property of the manager. Source 5 refutes the inference at the link-layer level, and this is the cleanest kill in the whole packet:\n\n- **Scanning and initiating are distinct, time-sliced link-layer procedures** (BLE LL spec; Microchip roles doc; US Patent 9,706,496 whose entire subject is that concurrent scanning and initiation must be *scheduled* against each other). A scan is receive-only: it delivers ADV reports to `didDiscover`. It never transmits `CONNECT_IND`.\n- An armed connect additionally requires the *initiator* to be armed for that specific peripheral, to win its own RX slot on the advertising event, to transmit `CONNECT_IND` at T_IFS, and **for the sensor to accept it.** Any of those can fail invisibly while the scanner keeps hearing adverts.\n\nSo \"my concurrent scan received the sensor's connectable advert during the window\" does NOT imply \"my armed connect should have completed.\" The two are independent paths, and one working tells you nothing about the other. **Worse for you: the corollary runs the opposite way.** If the *same* CBCentralManager is still delivering scan callbacks during a miss, the manager and its RX pipeline are *demonstrably alive*. That is evidence the manager is NOT poisoned — the specific initiator-arming or the peripheral object is the failure, not the manager as a whole. Your headline observation, read correctly, argues against your own conclusion.\n\nThere is also a sensor-side reason the connect legitimately shouldn't fire while ads continue: `CONNECT_IND` can be *refused* by a sensor that thinks its slot is still occupied (Source 4, Source 5). Advertising and accepting-a-connection are separate sensor behaviors. A G7 that still counts your half-released link against `maxDevices=3` will keep advertising (normal 5-min duty cycle) while refusing you. Ads-continuing is fully consistent with \"your slot is still held,\" which is the *competing* hypothesis, not yours.\n\n---\n\n## (d) Three consecutive misses on the build that ADDED the recreate-cure is direct disconfirming evidence\n\nThis is the most underrated item in your own brief. If recreating the central were the true cure, the build that automates the recreate should not produce runs of consecutive misses — the cure would fire and catch the next window. Three-in-a-row on that build is a straightforward failed prediction of your hypothesis. It is consistent with (b): the recreate is ~50% effective / coincidental with a sensor timeout, so on a bad run it simply doesn't help and you stack misses. A real object-replacement cure would not show that failure mode. Under Popper, this is the cleanest signal you have and it points *against* the conclusion you're being asked to defend.\n\n(One honest caveat, in your favor and against me: \"recreate then still degrades within a window or two\" could also mean the leak re-accumulates faster than the recreate clears it — which is compatible with *either* hypothesis. So (d) weakens \"recreate cures it\" but does not by itself decide between poisoned-central and slot-leak. The link-layer argument in (c) and the parsimony argument in (a) are what break the tie.)\n\n---\n\n## Two more independent strikes from the reference implementations\n\n- **No reference collector recreates the central. Ever.** xDrip4iOS keeps one central for the whole session (Source 1). xDrip+ keeps `rxBleClient` as a long-lived singleton and reaches for *active scan* first, with a full adapter-restart only as a rare 12-failure watchdog (Source 3). Juggluco throws away the per-connection GATT object every cycle but *never* toggles the adapter (Source 2). Loop's own G7SensorKit re-acquires by *scanning after a 2s delay*, never by rebuilding the manager (Source 6). Recreating the CBCentralManager per-degradation is a hammer nobody in the ecosystem swings — it maps to xDrip+'s last-resort adapter-restart watchdog, not to anyone's first-line fix. If central-recreation were the natural cure for \"connect path degrades while scan works,\" you'd expect at least one mature implementation to have converged on it. None did.\n\n- **The ecosystem's actual cure targets bond/slot state, not the manager.** Juggluco's wear recovery is `unbond()` + re-pair (Source 2). xDrip+'s is \"forget the DXCM/DX01/DX02 bond and wait ~15 min for rapid-reconnect\" (Source 3, Source 4). Both act on *connection/occupancy* state. Source 2 states the inference plainly: recovering by unbonding rather than rebuilding the central is evidence pointing *away* from \"the central is poisoned\" and *toward* \"connection/auth/occupancy state is what needs cycling.\"\n\n---\n\n## Verdict\n\n**Partly right, mislabeled — and the label is the problem.** The *phenomenon* you observed is real and documented (connect path degrades while scan path survives; iOS 11/17 stuck-connect reports match your symptom exactly). But your *root-cause attribution* — \"the manager object is poisoned, replacing it is the cure\" — is the weakest available explanation:\n\n- It requires an undocumented manager-level scan/connect split, when Apple-documented mechanisms (2-slot watchOS ceiling + nonblocking cancel that doesn't tear down the link + stale-peripheral-instance) already produce every symptom.\n- Its central evidentiary pillar — \"observer sees ads, so connect should have fired\" — is a link-layer category error; corrected, the same observation shows the manager is *alive*, not poisoned.\n- Its cure is confounded (fresh peripheral object, elapsed sensor-timeout, and manager-replacement all change together) and only ~50% effective, which is not how a true object-replacement cure behaves.\n- The recreate-cure build produced three consecutive misses — a direct failed prediction.\n\nMost likely root cause: **an incomplete BLE teardown on watchOS that leaves your G7 link half-open, consuming one of your app's two connection slots (and possibly one of the sensor's three), so the next armed connect is silently starved.** \"Poisoned central\" and \"connection didn't release\" are almost certainly the same defect described from two ends. Recreating the central works *when it works* because it force-frees your local slot and hands you a fresh peripheral — not because the manager was corrupt.\n\n---\n\n## The single falsifying experiment (test protocol, no code)\n\n**Instrument the watchOS 2-slot budget and the sensor's advertising cadence simultaneously across a natural miss — no recreate allowed.**\n\nRun a session and, whenever a window misses, do NOT recreate the central. Instead, during the blackout, log two things your existing \"131 window observer\" can already capture or be pointed at:\n\n1. **Inter-advertisement interval during the blackout.** 5-min spacing ⇒ the sensor still believes a client is healthy (your link is half-held) ⇒ slot-occupancy/client-side teardown fault. 1-min spacing (\"rapid reconnect\") ⇒ the sensor considers you gone and is soliciting you ⇒ the fault is purely local initiator/peripheral, and slot-occupancy is exonerated. (Source 4's discriminator.)\n\n2. **A slot-budget probe:** at the moment of a miss, attempt to enumerate/adopt via `retrieveConnectedPeripherals(withServices:)` and observe whether the G7 is still listed as *connected to the system*, and whether a *third* connection can be armed. If the G7 shows up as system-connected while your app can't connect to it, the link never tore down — teardown/slot, not poison.\n\n**What falsifies \"poisoned central\":** if, during a blackout with the *same, un-recreated* central, either (i) the sensor is advertising at 5-min cadence AND still enumerates as system-connected, or (ii) simply *waiting one additional sensor-timeout interval (~15 min) without any recreate* restores connectivity at the same rate recreation achieves — then the manager was not poisoned and the recreate was not the cause. The predicted-by-your-hypothesis result (recreate is *necessary*; waiting never recovers; the sensor is not still system-connected) is the only outcome that would keep \"poisoned central, recreate is the cure\" alive.\n\n**The even cheaper confirmatory half** (Source 4's second diagnostic): in a separate session, stop issuing your own `cancelPeripheralConnection` on the success path — let the sensor's own post-read disconnect land — and measure whether the `.disconnecting`-stuck rate and the subsequent miss rate drop. If they do, the self-issued teardown race is confirmed as the mechanism and \"recreate the central\" is treating a symptom.\n\nNo source refuted the *existence* of the stuck-connect phenomenon — that part of your observation is solid. What collapses under scrutiny is the *attribution* to a poisoned manager and the *causal* claim about recreation. On the current evidence, both are more likely wrong than right.",
      "Verified the linchpin in primary source (`G7BluetoothManager.swift:236–243`, Pete Schwamb's own comment). Here is the refutation.\n\n---\n\n# Refuting connect-per-reading — and where the refutation actually lands\n\n## The strongest case for a persistent connection (steelmanned)\n\nIf the watch could open the link once, run J-PAKE + cert + AES a single time, subscribe to the EGV/control/backfill characteristics, and then simply *hold the connection open and receive notifications*, the entire problem class you are fighting evaporates:\n\n- **No window-chasing.** You never have to predict the ~6s connectable burst, never arm a pending connect, never miss a window. The sensor pushes each EGV up an already-open, already-encrypted pipe.\n- **No teardown race.** There is no per-read `cancelPeripheralConnection`, so there is no `.disconnecting`-stuck peripheral, no half-open link, no leaked watchOS connection slot.\n- **No re-handshake cost.** You pay the 7–8s J-PAKE exactly once per session instead of every 5 minutes.\n- **No central-recreate churn.** One central, one peripheral object, standing subscriptions — the model xDrip4iOS uses and never has to recreate its stack for.\n\nOn paper this is strictly dominant. It removes the disconnect churn AND the re-handshake AND the miss rate in one move. If it were available, connect-per-reading would be indefensible.\n\n## Why it is not available: the sensor firmware forecloses it\n\nThe premise \"hold a persistent connection and receive notifications\" assumes the *G7 will keep the link up*. Every independent primary source says it will not:\n\n1. **Loop's own maintainer, in the code you build on** (`G7BluetoothManager.swift:236–243`, read directly): *\"Persistent connections don't seem to work with the transmitter shutoff: The OS won't re-wake the app unless it's scanning. The sleep gives the transmitter time to shut down…\"* This is not a design preference — it is a report that the transmitter powers its radio **off** between readings. There is nothing on the other end to hold a connection to.\n\n2. **The Juggluco author, on a shipping Wear OS watch:** *\"Dexcom G7/ONE+ disconnects also on phones after every glucose value.\"* Sensor-initiated, every platform.\n\n3. **xDrip4iOS — the one implementation that looks \"persistent\" — is not.** Its base class explicitly treats the periodic drop as normal (`// Expected short-lived disconnect (normal Dexcom behavior)`) and re-arms a `connect()` on the same peripheral every window. The *sensor* drives the disconnect rhythm; xDrip never gets to \"subscribe once and coast.\" So even the poster child for persistence re-establishes the link on the sensor's 5-min grid.\n\n**This is the decisive refutation of the refutation.** \"Persistent connection\" is not a mode the G7 offers. Asking whether we should switch to it is like asking whether we should keep a phone call open to a phone that hangs up after every sentence. The distinction between \"connect-per-reading\" and \"persistent\" largely collapses: *everyone* re-establishes each window because the firmware tears down each window. The only project that even approximates persistence does so by free-riding the Dexcom app's system-wide bond (xDrip4iOS is a bond-free coexistence *reader* — no J-PAKE at all), which is not a regime available to a sole-central watch.\n\nSo the verdict on the literal question — *should we switch to a held-open, notification-driven persistent connection?* — is **no, because the sensor will not permit it.** That path is a dead end regardless of watchOS.\n\n## The re-handshake justification is a non-sequitur — and this is where connect-per-reading *is* wrong\n\nNow the sharper point, and the one worth taking back to the team. Your standing justification — *\"the bonded reconnect shortcut left the link unencrypted, so we must re-run the full cert exchange every time\"* — is being used to justify the wrong thing.\n\nRe-running the cert exchange is a statement about **what you do when a new link comes up.** Self-issuing `cancelPeripheralConnection` after each read is a statement about **how the previous link ends.** These are independent. The sensor is going to drop you either way (fact 1–3 above). Therefore:\n\n- The re-handshake cost is **not** a consequence of, or a reason for, disconnecting. It is a consequence of the *sensor* dropping you. You would pay it whether or not you ever call `cancel` yourself.\n- \"We must re-handshake\" is thus **not a justification for connect-per-reading at all.** It is simply the price the firmware imposes. Using it to defend the self-disconnect is a category error: it defends a cost you cannot avoid to justify a behavior (proactive teardown) that you *can* avoid and that is actively harmful.\n\nWhat is harmful is the **proactive self-disconnect**, and here the evidence is strong (Sources 4, 5, and Loop's own comment converge):\n\n- `cancelPeripheralConnection` is documented-nonblocking and, on watchOS especially, does **not** guarantee the physical link drops. Issuing it at the same instant the sensor's radio is already going dark is a race: the LL-terminate handshake can't complete → `CBPeripheral` wedged in `.disconnecting` for >5s → the next armed `connect()` against the wedged object is silently swallowed. That is precisely your field signature.\n- Loop's fix for exactly this is in the code you already own: on the sensor's disconnect, **`Thread.sleep(2s)` to let the transmitter finish shutting down, then SCAN and connect on discovery** — never self-cancel, never re-arm instantly, never recreate the central.\n\nSo the correct architectural change is **not** \"persistent vs. connect-per-reading.\" It is a set of three changes *within* the (mandatory) re-establish-per-window model:\n\n1. **Stop self-issuing the post-read disconnect.** Let the sensor drop the link. This is the single highest-value change and directly targets the `.disconnecting`-stuck / leaked-slot mechanism. (Cheap experiment: stop calling `cancel` on the success path and measure whether the stuck rate falls — Source 4's proposed test.)\n2. **Replace the armed-pending-connect-without-scan with scan-then-connect-on-discovery.** Your own data shows the scan path is the one that still works during a miss; Loop and xDrip both ride the scan path deliberately. Arming a connect on the initiator path while never scanning is riding the path your field data proves is dead.\n3. **Keep the bonded/auth shortcut, but re-handshake *on the reconnect* rather than treating \"unencrypted link\" as a reason to bail.** Re-handshaking on a fresh link is normal and correct; it is not a reason to disconnect. If OS-level bonding can be made to persist the LTK (as Juggluco does — full J-PAKE once, short AES challenge thereafter), you additionally shorten each window's link occupancy, easing slot pressure. That is an optimization, not a prerequisite.\n\n## Could persistent avoid both the churn and the re-handshake? \n\nOnly if the sensor held the link — which it does not. So: the churn is avoidable (stop self-disconnecting), and the re-handshake is *not* avoidable per-window but *is* shortenable (bonded AES shortcut). You get most of the imagined benefit of \"persistent\" without needing persistence, by ceasing to fight the teardown and by not re-arming into a wedged stack.\n\n## Is persistence viable on watchOS specifically?\n\nIndependently moot, but worth recording: even if the G7 *would* hold a link, watchOS caps BLE at **2 connections per app** (Apple-staff confirmed). You already hold the Omnipod link — that is 1 of 2. A held-open G7 link would consume the second slot permanently, and any leaked/half-open state would leave you at the ceiling with the next connect silently never firing. watchOS background BLE is also driven by scanning re-wake (Loop's comment: *\"the OS won't re-wake the app unless it's scanning\"*), which structurally favors the scan-then-connect model over a parked connection. So watchOS independently disfavors persistence — but the firmware already settled it.\n\n## Verdict\n\n**Do not adopt a persistent, notification-driven connection — it is foreclosed by the G7 firmware (transmitter shutoff after every EGV), corroborated by Loop's own maintainer comment, the Juggluco author, and xDrip's \"expected disconnect\" handling. The premise that persistence would \"eliminate window-chasing\" is false because no client is permitted to persist.**\n\n**But connect-per-reading as *you currently implement it* should still change.** The re-handshake justification does not defend the part that is actually hurting you: the proactive self-disconnect and the armed-connect-without-scan. The architecture to move to is the one already sitting in your own `G7SensorKit` and mirrored by xDrip and Juggluco: **let the sensor initiate the teardown, insert a post-disconnect settle delay, then re-acquire by active scan and connect-on-discovery — not by parking a pending connect and not by recreating the central.** That keeps the mandatory per-window re-establishment while removing the self-inflicted teardown race, the dead-path reliance, and the central-recreate hammer.\n\nNet: the *label* \"connect-per-reading\" is correct and must stay; the *implementation choices bundled under it* (self-cancel, no-scan arm, treat-unencrypted-as-abort) are the refutable part, and they should go.\n\nPrimary source verified this session: `/Users/jeremybarnum/Downloads/Loop/March2026/LoopWorkspace-fromstock/G7SensorKit/G7SensorKit/G7CGMManager/G7BluetoothManager.swift:236–250` (transmitter-shutoff comment; `scanAfterDelay` → 2s sleep → scan model)."
    ],
    "research": [
      "I now have decisive evidence from the actual source plus the official docs. Here are my findings.\n\n---\n\n# How xDrip4iOS reads the Dexcom G7 / ONE+ / Stelo over BLE\n\n**Sources (all fetched and read directly):**\n- `xDrip/BluetoothTransmitter/CGM/Dexcom/G7/CGMG7Transmitter.swift` (722 lines) — the G7 subclass\n- `xDrip/BluetoothTransmitter/Generic/BluetoothTransmitter.swift` (956 lines) — the CoreBluetooth base class\n- Official docs: https://xdrip4ios.readthedocs.io/ (G7/ONE+/Stelo compatibility page)\n- Provenance confirmed: pulled from `JohanDegraeve/xdripswift` master via GitHub API; the G7 file's most recent commits are by Paul Plant (core maintainer), Mar 2026. This is genuine current upstream, not a fork.\n\n## The single most important finding (read this first)\n\n**xDrip4iOS's G7 path is a passive \"coexistence\" reader, not a standalone sensor client.** It does NOT do the pairing at all — no J-PAKE, no AES auth, no certificate exchange, no proof-of-possession, no bond-driving. There is zero pairing/handshake code in the G7 file or anywhere in the repo (grepped the whole tree: the only Dexcom pairing code is the legacy `G6Firefly/` for G5/G6; nothing for G7).\n\nThe official docs state it plainly: the G7 is *\"Compatible (Dexcom G7 app must be running at the same time)\"* and *\"the main connection between the transmitter and your iPhone is handled by the Dexcom app. xDrip4iOS will simply receive a copy of the transmitted data.\"*\n\nMechanically: the **Dexcom app** holds the bond/encryption. iOS shares that bond system-wide, so when xDrip opens its own second connection to the same peripheral, the encrypted notify characteristics are already readable **without xDrip re-authenticating**. In `CGMG7Transmitter.swift` the auth characteristic is only *read* to check status flags — line 432–434: on receiving `AuthChallengeRxMessage`, `if authChallengeRxMessage.paired, authChallengeRxMessage.authenticated { … \"Will stay connected to this one.\" }`. If the link is *not* already paired/authenticated by the other app, xDrip does nothing and lets a timeout drop it (line 450: *\"Waiting briefly for data (coexistence)\"*). It never drives pairing.\n\n**Consequence for your watch project:** xDrip is *not* a clean architectural precedent for your app. Your watch is the sole central and must run the full ~7–8 s J-PAKE + bond every connection. xDrip has **no per-connection handshake cost at all** — that's the whole reason its persistent-connection model is cheap. Keep this caveat front-and-center when reasoning from xDrip.\n\nWith that framing, the four specific answers:\n\n## 1. Persistent connection + notifications, or connect-per-reading?\n\n**Persistent connection with periodic notifications. It does NOT disconnect after each EGV.** High confidence — verified in source.\n\n- On connect (`CGMG7Transmitter.didConnect`, line 469–484) it discovers services, then in `didDiscoverCharacteristicsFor` (line 486–527) issues `setNotifyValue(true, …)` on the Authentication, Write/Control, and Backfill characteristics (lines 513–522), then simply waits.\n- When a glucose reading arrives (`peripheralDidUpdateValueFor`, `.glucoseG6Tx` case, lines 286–363), it parses and delivers it to the delegate — and there is **no `disconnect()` call anywhere in that path**. The connection is left open.\n- The only `disconnect()` in the entire G7 subclass is the error path for an \"Encryption is insufficient\" notify failure (line 542–556). Normal readings never trigger a disconnect.\n\n## 2. If persistent: how is it maintained across the schedule? Re-subscribe or stay connected?\n\n**It stays connected and subscribes once per connection; it does not re-subscribe per 5-min cycle.** But the *sensor* still drops the link periodically, and xDrip handles that by immediately re-arming a connect on the same peripheral. High confidence — verified.\n\n- Subscriptions are set once, in `didDiscoverCharacteristicsFor`, and left in place.\n- The G7 periodically tears down the BLE link on its own. xDrip's base class treats this as expected — `didDisconnectPeripheral`, line 655–657: `if let cbErr = err as? CBError, cbErr.code == .peripheralDisconnected { // Expected short-lived disconnect (normal Dexcom behavior) }`.\n- On that disconnect, the base class re-issues a connect on the **same** `CBPeripheral` object (line 688–690): `if shouldReconnectOnNextDisconnect, let ownPeripheral = self.peripheral { … centralManager?.connect(ownPeripheral, options: connectOptions) }`. This is a **pending/armed connect** — no scan — that iOS completes the next time the sensor is connectable. The design comment (line 471) captures the intent: *\"iOS will connect as soon as the peripheral comes in range.\"*\n\nSo the maintenance model is: **one long-lived central, one long-lived peripheral object, and a standing outstanding `connect()` that iOS re-satisfies each advertising window.** The sensor drives the disconnect/reconnect rhythm; xDrip never disconnects itself and never recreates the central.\n\n## 3. How does it discover / reconnect — scan, connect(options), or retrieveConnectedPeripherals?\n\nAll three of the classic paths appear, in this priority order (base class). High confidence — verified.\n\n- **Primary reconnect to a known sensor: `retrievePeripherals(withIdentifiers:)` then `connect()`.** `connect()` (line 216–223) calls `retrievePeripherals` (line 474–498): `central.retrievePeripherals(withIdentifiers: [uuid])` → `central.connect(peripheral, options: connectOptions)`.\n- **First-time discovery falls back to `scanForPeripherals(withServices:)`** (line 314), filtered by the G7 advertisement UUID `FEBC` / service `F8083532-…`.\n- It does **NOT** use `retrieveConnectedPeripherals(withServices:)` (the \"already-connected-to-the-system\" API). Notable, since in coexistence the Dexcom app has it connected — xDrip instead keeps/uses the peripheral's own UUID.\n- `connectOptions` (line 118–120) = `[CBConnectPeripheralOptionNotifyOnConnectionKey: true, CBConnectPeripheralOptionNotifyOnDisconnectionKey: true]`. **No `CBConnectPeripheralOptionEnableAutoReconnect`** (the iOS 17+ key) — it relies on the classic \"an outstanding `connect()` never times out\" behavior.\n- Background/kill survival is via **CoreBluetooth state restoration**, not scanning: the central is created with `CBCentralManagerOptionRestoreIdentifierKey` (line 870), and `willRestoreState` (line 783–810) re-attaches the restored peripheral and, if still connected, re-discovers services to re-subscribe. (Your watch uses an HKWorkoutSession for background survival instead — a different mechanism.)\n\n## 4. Any comment/logic about the G7 dropping the connection after each reading, or about connection slots?\n\n**On dropping the connection: yes — one explicit comment, treating it as normal.** On connection slots: **no — nothing.** High confidence.\n\n- Dropping: `BluetoothTransmitter.swift` line 656, `// Expected short-lived disconnect (normal Dexcom behavior)`. That is the only such comment, and it frames the periodic disconnect as routine, not a fault.\n- The G7 file also has a comment about the sensor *\"keep[ing] sending/repeating the same BG value via BLE even after the session officially ends\"* (line 315) — but that's about expiry, not slots.\n- **No mention of connection slots, `maxDevices`, a device-list response, or the sensor refusing a connect because a slot is occupied.** xDrip never reasons about slots. Its `temporarilyRejectedDeviceNames` cooldown (base class, line 114–133, 180 s) is xDrip's *own* logic to avoid latching onto a stale/wrong nearby \"DX…\" transmitter during discovery — unrelated to sensor-side slot accounting.\n- xDrip also has **no handling for a peripheral \"stuck in .disconnecting\"** — no special-casing of that state at all. Its disconnect is a plain `cancelPeripheralConnection` (line 240), and it never overlaps its own disconnect with a fresh connect the way a connect-per-reading loop would.\n\n## What this does and doesn't tell you about your watch problem\n\nLegitimately supported by the evidence:\n- **xDrip never recreates the CBCentralManager.** One central for the whole session; the periodic disconnect is handled with a simple re-`connect()` on the same peripheral. This is a real counterexample to \"you must recreate the central\" — but note xDrip may be sidestepping the poisoning entirely because it isn't the connection's owner/authenticator.\n- **xDrip is architecturally the opposite of connect-per-reading:** it keeps a standing outstanding `connect()` and lets the *sensor* drive the disconnect. It never issues its own post-read disconnect, so it never creates a disconnect→reconnect overlap. This is consistent with your competing hypothesis that a self-issued disconnect after each read (and a not-fully-released connection) can be self-inflicting. (Corroborating but weaker: xDrip's *legacy G5/G6 Firefly* path — which was genuinely connect-per-reading — is the one with a documented history of reconnect-loop/battery-drain pathology, GitHub issue #494; the G7 path was deliberately built differently.)\n\nWhere xDrip does NOT transfer, and you must not overclaim:\n- **Platform:** iOS phone, not watchOS. Different background/radio model.\n- **No handshake:** xDrip free-rides on the Dexcom app's bond, so its per-window work is trivial (link re-establish + notify). Your watch does a full standalone J-PAKE each time. xDrip therefore says nothing about whether a *standalone, per-connection-handshaking* central can hold a persistent connection cheaply.\n- **Slot dynamics differ:** with the Dexcom app present, the bond persists and the sensor sees an already-trusted phone. Your watch is the sole central doing its own bond gate each cycle — exactly the regime where a not-fully-released connection could plausibly still occupy one of the sensor's 3 slots. xDrip's success does not rule this out; it simply never enters that regime.\n\n**Net:** xDrip4iOS confirms that the *reliable* way to read a G7 is a persistent connection with standing notifications and a re-armed `connect()` on the same peripheral — never central-recreation, never a self-issued per-read disconnect. But it achieves this as a bond-free coexistence reader, so it is evidence *against* central-recreation and *against* connect-per-reading churn, not evidence that a standalone handshaking watch central can trivially do the same. The one architecture xDrip has genuinely never had to solve is the one you're attempting: sole central, full handshake, persistent link.\n\nRelevant local copies I saved for reference: `/private/tmp/claude-501/-Users-jeremybarnum-Downloads-Loop-March2026-LoopWorkspace-fromstock--claude-worktrees-gifted-shirley-c9c3dc/f5d0a673-76df-4bc5-826e-ebcacd4499a1/scratchpad/CGMG7Transmitter.swift` and `/private/tmp/claude-501/-Users-jeremybarnum-Downloads-Loop-March2026-LoopWorkspace-fromstock--claude-worktrees-gifted-shirley-c9c3dc/f5d0a673-76df-4bc5-826e-ebcacd4499a1/scratchpad/BluetoothTransmitter.swift`.",
      "I have enough from primary source (the actual Java/C++ in the repo) plus the author's own docs and wiki. Here are the findings.\n\n---\n\n# How Juggluco connects to & reads the Dexcom G7 over BLE — including Wear OS\n\n**Repo / authorship (verified):** Juggluco is by Jaap Korthals Altes (`j-kaltes`), GPLv3, at github.com/j-kaltes/Juggluco (not jamorham — that's xDrip+). I cloned `primary` and read the actual source. The Dexcom logic lives in two places: native C++ crypto/parsing in `Common/src/main/cpp/dexcom/` (J‑PAKE, AES, cert/message handling) and the BLE state machine in Java at `Common/src/dex/java/tk/glucodata/DexGattCallback.java`, with the shared connection driver in `Common/src/main/java/tk/glucodata/{SuperGattCallback,SensorBluetooth}.java` and the wake-alarm receiver `Common/src/dex/java/tk/glucodata/ConnectReceiver.java`.\n\n## 1. Connect-per-reading, NOT a persistent connection — confirmed in source\n\nJuggluco uses the **same connect-per-reading model you do.** It is not a held-open connection with steady notifications.\n\n- **The connection is torn down after every single EGV.** In `DexGattCallback.onConnectionStateChange`, the `STATE_DISCONNECTED` branch is where the *next* read is scheduled — there is no persistent link. Critically, the successful-data path (`getdata`, opcode `0x4E`) contains **no client-side `disconnect()` call** — Juggluco processes the one EGV, asks for backfill, and the link ends because **the sensor drops it.** The author states this explicitly on the project wiki: *\"Dexcom G7/ONE+ disconnects also on phones after every glucose value.\"* (Samsung Galaxy Watch7 section). So the drop is sensor-initiated and happens on every platform.\n\n- **Direct connect, not a long-lived armed/pending connect.** `SuperGattCallback.getConnectDevice()` calls `device.connectGatt(app, autoconnect, cb, TRANSPORT_LE)` with `autoconnect=false` by default (`SuperGattCallback.java:65`). That's an Android *direct* connect (≈30 s stack timeout), fired at the right moment — the opposite of parking a permanent `autoConnect=true` pending connect. (There *is* an opt-in `autoConnect=true` mode, gated behind a settings toggle labeled \"Android 13\": `SensorBluetooth.java:946` `autoconnect = Natives.getAndroid13()`, and `bluediag.java:482`. Default off.)\n\n- **Timing is done with an EXACT ALARM aligned to the 5-min grid.** On disconnect, if `getalarmclock()` is on (`DexGattCallback.java:272-280`):\n  ```\n  stillwait = 5*60*1000 - alreadywaited - 27500;   // alreadywaited = now - datatime(last EGV)\n  setalarm(now+stillwait, ...)  →  AlarmManager.setAlarmClock(...)\n  ```\n  i.e. it re-arms a direct connect **~27.5 s before the next expected EGV** (your ≈45 s pre-window arm is the same idea). Without the alarm-clock option it just reconnects ~7 s later (`stillwait = 7000 - alreadywaited`). `ConnectReceiver.onReceive` → `connectNamedDevice` → `connectToActiveDevice` → `connectGatt`.\n\n- **It fully closes and recreates the per-connection object every cycle.** `SuperGattCallback.close()` does `gatt.disconnect(); gatt.close(); mBluetoothGatt=null;`, and each window makes a brand-new `connectGatt`. `resetconnect()` also disables every notification and resets the handshake phase. **Note the important nuance for your poisoned-central debate:** Android's per-connection object is `BluetoothGatt`, and Juggluco *always* throws it away and makes a fresh one — but it does **not** recreate the `BluetoothAdapter`/BLE manager (the CBCentralManager analog). So Juggluco's normal behavior is closer to \"never reuse a peripheral/GATT client\" than to \"recreate the central.\" It gets a clean connection object for free, every read, by design.\n\n- **Scan is only a fallback to (re)acquire the address.** `connectToActiveDevice` calls `connectDevice(...)` first; only `if(!connectDevice && !mScanning)` does it `scanStarter(...)` (`SensorBluetooth.java:139-144`). The scan path exists to rediscover the device address when it's null/lost — it is not the steady-state read mechanism. This mirrors your \"armed connect primary, passive scan as fallback/diagnostic.\"\n\n- **Big efficiency difference worth flagging: Juggluco does NOT re-run full J‑PAKE every reading.** It BONDs at the OS level (`createBond` via reflection, `TRANSPORT_LE`) and persists auth/keys natively. On a known, bonded sensor the descriptor-write callback (`DexGattCallback.java:132-142`) does:\n  ```\n  if (bonded && Natives.isAuthenticated(dataptr)) { phase=RequestAuth; requestAuth(); }  // skip J-PAKE\n  else                                             { docmd0(...); }                        // full J-PAKE rounds\n  ```\n  So full J‑PAKE rounds 0/1/2 + certificate exchange run **once** (first pairing, saves device name + keys, `saveDeviceName`/`dexKnownSensor`), and every subsequent per-reading connect does only the **short AES challenge → getdata**. If your from-stock app is re-running the FULL J‑PAKE + certificate + proof-of-possession sequence on *every* 5-min connect, that is heavier and holds the link/slot materially longer than Juggluco does — a plausible contributor to your \"stuck in .disconnecting\" / slot-occupancy symptoms. (Juggluco leans on the BLE bond's LTK so the link re-encrypts fast without redoing the asymmetric handshake.)\n\n**GATT profile it drives** (from source, for cross-checking your implementation): service `f8083532-849e-531c-c594-30f1f86a4ea5`; characteristics `…3534` (auth/control), `…3535` (control-with-auth), `…3536` (backfill), `…3538` (certificate); scan filter service `febc`. Uses hardcoded Dexcom client certificates whose DER subjects are ASCII `DEX00PG1` / `DEX03PG1` — i.e. it presents as a legitimate Dexcom client identity. Same crypto family your app uses.\n\n## 2. Wear OS: yes, the watch connects DIRECTLY to the G7, using the SAME model\n\n- **The G7 BLE stack is compiled into the watch app.** `Common/build.gradle` declares orthogonal flavor dimensions `['wearos','libreVersion','SiBionics','DexCom','Google']`; `dex` is a `DexCom`-dimension flavor and `wear` is a `wearos`-dimension flavor, and a real combined variant exists (`wearLibre3SiDexNogoogle`, build.gradle:568). Because Android product-flavor source sets are additive, a `wear…Dex…` build includes `src/dex/java` (DexGattCallback/ConnectReceiver) — the identical connect-per-reading code the phone runs.\n\n- **Same code, with watch-specific tuning via `isWearable` branches.** `getConnectDevice()` has an explicit `if(isWearable){ connectGatt(...) }` branch (`SuperGattCallback.java:564`). In `DexGattCallback`, wear paths are *more aggressive*: it `unbond()`s after a stuck handshake (`foundtime+8min`), and gives up after 1 failed attempt on wear vs 4 on phone (`triedinvain > (isWearable?1:4)`, line 254). The **exact-alarm wake is the watch's key addition** — changelog v9.0.13: *\"Added connect timer for Dexcom G7 sensors, to awake a WearOS watch to make a connection to the sensor,\"* and the in-app help: *\"Let Juggluco use an alarm clock function to ensure the correct timing of connecting… Without it, when the device is in deep sleep… it will sometimes not awake to reconnect.\"* It also holds a `PARTIAL_WAKE_LOCK \"Juggluco::Dexcom\"` across each ~7-8 s connection (`getlock()`/`releaselock()`).\n\n- **Default is phone-relay; direct-to-watch is an explicit toggle.** README line 13: *\"The phone version of Juggluco scans the sensor and sends this data to the WearOS version… After initialization, you can switch on and off directly connecting the sensor with the watch, with left menu→Watch→Wear OS config→Direct sensor-watch connection.\"* Wiring: `Wearos.java:100/308` and native `Natives.directsensorwatch(String ident)`. So: the **phone is required for initial provisioning** (a *camera* scan of the applicator's data-matrix to obtain the sensor identity/pairing secret — not a BLE scan), after which \"Direct sensor-watch connection\" lets the **watch do the G7 BLE itself**, connect-per-reading, exactly like the phone. Confidence that direct mode = same model: high (read the code). Confidence on whether the watch runs fully phone-independent afterward: medium — the toggle clearly moves the BLE onto the watch, but I found no explicit \"works with phone powered off\" statement in Juggluco's own docs.\n\n## 3. G7 connection lifecycle — the sensor drops the link between readings\n\nPrimary-source answer, from the author: **the G7 does not hold the connection open — it disconnects after each glucose value, on phone and watch alike** (\"Dexcom G7/ONE+ disconnects also on phones after every glucose value,\" wiki). Corroborated in code by the absence of any client disconnect on the data path, plus the backfill-keepalive changelog notes: v9.0.6 *\"Keeps Dexcom G7 sensor with only old data somewhat longer connected via Bluetooth to receive old data\"* (i.e. it *deliberately* lingers only for backfill; normally the link closes right after the current EGV). This is strong external confirmation that your connect-per-reading architecture matches the sensor's actual behavior — the 5-min drop is the sensor's design, not a bug you introduced. (Juggluco reads exactly one live EGV per window via char `…3538` notify + `0x4E`, then optional backfill via `…3536`.)\n\n## 4. Multiple clients / maxDevices=3 — Juggluco does essentially nothing special\n\nThere is **no slot-management or maxDevices logic in Juggluco's source.** (`maxDeviceName=39` in `settings/GlucoseMeter.hpp` is just a name-string buffer, not a device count — the only near-hit.) Juggluco:\n- pairs/bonds as **one client identity** (the `DEX00PG1/DEX03PG1` certificates), occupying a single pairing slot;\n- **holds a *connection* slot only for the ~7-8 s handshake+read**, then the sensor drops it — so concurrent-connection contention is minimized structurally by the connect-per-reading design, whether or not other clients (Dexcom app, receiver, another phone) are also paired;\n- has no code to detect/avoid \"too many devices,\" negotiate slots, or coexist with a specific Dexcom receiver/app. It simply relies on the fact that it isn't holding a slot most of the time.\n\nSo on the maxDevices=3 question, Juggluco is not a source of clever handling — its answer is \"bond once, connect briefly, disconnect, repeat,\" which keeps it out of the concurrency ceiling by construction. Worth internalizing: the relevant limit is **paired/bonded devices (the 3-slot pairing cap)** vs **momentary concurrent connections** — Juggluco consumes one of the former and is transient on the latter.\n\n---\n\n## Bottom line vs. your architecture\n\nJuggluco independently arrives at the *same top-level design* you have — armed/direct connect-per-reading, ~5-min grid, scan only as address-recovery fallback, sensor drops after each EGV — which is external validation that connect-per-reading is correct for the G7. But three concrete differences stand out as things that could explain your miss rate:\n\n1. **It never re-runs full J‑PAKE.** Bond + persisted auth means each per-reading connect is a *short* AES auth, not the full J‑PAKE+cert exchange. Re-doing the whole handshake every window (if that's what you do) lengthens link occupancy and could aggravate the \"stuck disconnecting\"/slot symptoms.\n2. **Its \"recreate\" is the per-connection GATT object, every cycle — not the manager.** It always `disconnect()+close()+null`s and makes a fresh `connectGatt`; it never toggles the adapter. Your CBCentralManager-recreate is a heavier hammer than Juggluco ever swings — the cheaper equivalent is simply never reusing a peripheral/connection object.\n3. **Direct connect (autoConnect=false) fired by an exact alarm, ~27.5 s pre-window** — not a permanently parked pending connect. Plus aggressive `unbond()` on wear after a stuck/slow handshake, which is exactly the failure mode (\"stuck disconnecting\" / next window won't connect) you're seeing; their recovery is to drop the OS bond and re-pair, not to rebuild the central.\n\nUncertainty I'm flagging honestly: the exact reason the *sensor* refuses your next connect (poisoned central vs. lingering slot) can't be resolved from Juggluco's code alone — but the fact that Juggluco (a) bonds and skips re-J‑PAKE, and (b) recovers by unbonding rather than rebuilding the central, is evidence pointing away from \"the central is poisoned\" and toward \"connection/auth state (bond + occupancy) is what needs cycling.\"\n\n**Sources**\n- Source (read directly, cloned `primary`): `Common/src/dex/java/tk/glucodata/DexGattCallback.java`, `Common/src/main/java/tk/glucodata/SuperGattCallback.java`, `Common/src/main/java/tk/glucodata/SensorBluetooth.java`, `Common/src/dex/java/tk/glucodata/ConnectReceiver.java`, `Common/build.gradle`, `Common/src/mobile/java/tk/glucodata/Wearos.java` — https://github.com/j-kaltes/Juggluco\n- Author docs/changelog: https://www.juggluco.nl/Juggluco/changes.html (v9.0.6, 9.0.13, 9.0.15), README (line 13, WearOS/Direct sensor-watch), https://www.juggluco.nl/Jugglucohelp/sensorhelp.html (alarm-clock timing)\n- Project wiki (author statement the G7 disconnects after every value): https://github.com/j-kaltes/Juggluco/wiki\n\n(Local clone for your reference: `/private/tmp/claude-501/-Users-jeremybarnum-Downloads-Loop-March2026-LoopWorkspace-fromstock--claude-worktrees-gifted-shirley-c9c3dc/f5d0a673-76df-4bc5-826e-ebcacd4499a1/scratchpad/Juggluco`)",
      "I now have ground-truth from xDrip's actual source. Here are my findings.\n\n---\n\n# How xDrip+ handles the Dexcom G7 — evidence for the watchOS connection problem\n\n**Sources are the live master branch of `NightscoutFoundation/xDrip`.** I downloaded and grepped the two authoritative files rather than trusting doc prose (the docs contradict each other — the Intermittent page says \"scans every five minutes,\" the G7 page implies a persistent connection; neither is the real answer). Local copies saved at:\n- `/private/tmp/claude-501/-Users-jeremybarnum-Downloads-Loop-March2026-LoopWorkspace-fromstock--claude-worktrees-gifted-shirley-c9c3dc/f5d0a673-76df-4bc5-826e-ebcacd4499a1/scratchpad/Ob1G5CollectionService.java` (2628 lines)\n- `/private/tmp/…/scratchpad/Ob1G5StateMachine.java` (2184 lines)\n\nRaw URLs: [Ob1G5CollectionService.java](https://raw.githubusercontent.com/NightscoutFoundation/xDrip/master/app/src/main/java/com/eveningoutpost/dexdrip/services/Ob1G5CollectionService.java) · [Ob1G5StateMachine.java](https://raw.githubusercontent.com/NightscoutFoundation/xDrip/master/app/src/main/java/com/eveningoutpost/dexdrip/g5model/Ob1G5StateMachine.java)\n\nG7 runs through the *same* OB1 collector as G5/G6, distinguished by `shortTxId()` (a G7 uses a short transmitter ID). So this code IS the G7 path.\n\n---\n\n## 1 & 2. Connect-per-reading vs persistent — xDrip is ALSO connect-per-reading (HIGH confidence)\n\nxDrip does **not** hold a persistent connection open across the 5-minute gap. After each read it tears the connection down and re-arms for the next window — exactly your architecture:\n\n- After a successful read → `CLOSE` state → `prepareToWakeup()` (`Ob1G5CollectionService.java:1030`). That method schedules the next wake at **`anticipated_next_window − 15s`** using `DexSyncKeeper.anticipate()` (lines 1032–1036), or falls back to **285s (4m45s)** if it has no timing model yet (line 1038), then sets `state = CLOSED` and calls **`stopConnect()`** (lines 1048–1049) — the connection is dropped between readings.\n- Your \"~45s before the predicted window\" arming is the direct analog of xDrip's `anticipate − 15s` scheduling. Same design.\n- The **only** exception is the `always_connect` flag, which is hard-coded true for exactly one legacy device, the `\"G Watch\"` (`alwaysConnectModels`, line 261; set at line 1124). For all G7 users it disconnects every cycle.\n\n**Conclusion:** connect-per-reading is not inherently wrong — it is the production model xDrip has shipped for years. This refutes the framing that the architecture is \"self-inflicting\" merely by virtue of being connect-per-reading.\n\n---\n\n## The key difference: xDrip disconnects at the *application layer*, not the OS layer (HIGH confidence — most actionable finding)\n\nThis is the single most important thing I found, and it directly supports your **competing hypothesis** (that your connection isn't fully releasing and the sensor keeps counting you as occupying a slot).\n\nxDrip does **not** end a cycle by calling the OS \"cancel connection\" primitive. In `Ob1G5StateMachine.disconnectNow()` (`Ob1G5StateMachine.java:1048–1073`) it does:\n\n```\n// tell device to disconnect now\nconnection.writeCharacteristic(Control, new DisconnectTxMessage().byteSequence)\n```\n\nIt **writes a Dexcom `DisconnectTxMessage` to the encrypted Control characteristic** — an application-layer \"I'm leaving now\" command — and lets the **sensor** perform the BLE teardown. The resulting disconnect is then treated as the clean, expected path: a `BleDisconnectedException` while in `CLOSE`/`CLOSED` is logged as **\"Closed OK\"** (`CLOSED_OK_TEXT`, lines 378–382). There's even a guard-timed `inevitableDisconnect()` (lines 1039–1045) ensuring this command is always sent before teardown.\n\nWhy this matters for you:\n- xDrip deliberately makes the **sensor** initiate the teardown so the sensor *knows* the client has released and frees its slot (G7 `maxDevices=3`).\n- If your from-stock watch app ends each cycle with a bare `CBCentralManager.cancelPeripheralConnection()` and **does not** first write the Dexcom disconnect opcode over the control characteristic, the teardown is one-sided. The sensor may never observe a clean client-initiated close — which is precisely consistent with (a) your `.disconnecting`-stuck-for-5s CBPeripheral signature and (b) the sensor continuing to advertise while refusing your next armed connect because it still counts you against its 3 slots.\n- **I have NOT read your app's disconnect code** — this is a hypothesis to verify in your own codebase, not a confirmed fact about your app. But it is a concrete, source-backed mechanism, and it's the first thing I'd check: *does the from-stock app send the protocol-level DisconnectTxMessage before cancelling the GATT connection?* xDrip's authors clearly considered a unilateral OS disconnect insufficient.\n\n---\n\n## 3. Reconnect mechanism: direct connect to known MAC, NOT the pending/\"armed\" connect (HIGH confidence)\n\nAndroid has two connect modes and they map onto your iOS choices:\n- `autoConnect = false` = **direct connect**: bounded, fires immediately against a known MAC. No CoreBluetooth equivalent that's purely this.\n- `autoConnect = true` = **pending/background (\"whitelist\") connect**: opportunistic, indefinite, no active scan — **this is the analog of your CoreBluetooth armed `connect(peripheral)` with no scan.**\n\nxDrip's default reconnect each window is the **direct** connect:\n- `CONNECT_NOW` → `connect_to_device(false)` (`Ob1G5CollectionService.java:366–381`, esp. line 380) → `establishConnection(auto=false)` (line 689).\n- The pending/background path (`auto=true`, `CONNECT` state, line 384) is a *fallback only*, gated behind `use_auto_connect`, which **defaults false** (line 249) and is essentially never flipped on — corroborated by dev commentary in [discussion #4489](https://github.com/NightscoutFoundation/xDrip/discussions/4489) that `use_auto_connect` \"never gets changed.\"\n\n**Implication:** the mode you're relying on (armed pending connect, no scan) is the mode xDrip mostly *avoids*. xDrip fires a direct connect timed by `DexSyncKeeper` to hit the window, and — critically — escalates to active scanning on failure (next section). On iOS the closest equivalent to xDrip's primary strategy is **scan for the advertisement, then connect on discovery** — not a standing armed connect.\n\n---\n\n## 4. Failure handling — xDrip has your exact \"connect path dead, scan path alive\" failure mode, and its fix is to SCAN, not recreate the stack (HIGH confidence)\n\nThis is strong external corroboration of your suspected conclusion — with an important divergence on the remedy. `rxBleClient` in xDrip is a **long-lived singleton; it is never re-instantiated per window.** Instead there is an escalation ladder:\n\n1. **Switch to permanent active scan on connect failures.** `handleWakeup()` (lines 1011–1027): if `connectFailures > 0` (or `connectNowFailures > 0` without auto-connect), it sets `always_scan = true` and switches to the SCAN path, logging literally: **`\"Switching to scan always mode due to connect failures metric\"`** (lines 1016–1019). This is xDrip encoding your exact observation — the direct/pending connect can die while the device is still present — and its first-line cure is *active scan → connect on discovery* (`scan_for_device`, line 566 → `onScanResult` → CONNECT).\n2. **Missed-window detection also flips to scan.** `onConnectionStateChange` (lines 1604–1611): if a connection finally completes but took 310–620s (i.e., it silently missed a 5-min window), it sets `always_scan = true` — **`\"Connection time shows missed reading, switching to always scan\"`**. Direct evidence that the non-scanning connect path silently misses windows and scanning is the fix.\n3. **GATT cache refresh.** On a `CONNECT`-state failure it calls `tryGattRefresh()` (reflection `BluetoothGatt.refresh()`, clears the GATT service cache) then → SCAN (lines 1518–1526, 1531–1554).\n4. **Full Bluetooth adapter restart as a watchdog.** Every 12 consecutive `connectNowFailures` it calls `JoH.niceRestartBluetooth()` — toggles the entire BT adapter off/on (lines 1493–1496).\n5. **Service restart** after 30 min with no connection (lines 347–351).\n\n**Bearing on your \"recreate the CBCentralManager cures it\" conclusion:** xDrip's ladder confirms the *phenomenon* (connect path degrades while scan path works) is real and known. But note the ordering: xDrip reaches for **active scanning first** (cheap) and only toggles the whole adapter as a periodic watchdog (heavy). Recreating the `CBCentralManager` on watchOS is closest in spirit to xDrip's **adapter-restart watchdog**, not its first-line fix. Your data (\"a fresh central works then degrades\") is consistent with a poisoned host BLE stack — but xDrip's experience suggests the cheaper, more robust remedy is to **run an active scan and connect on the advertisement you can already see**, rather than tearing down and rebuilding the central every window.\n\n---\n\n## 5. Connection slots / bonding / the \"3 devices\" limit (MEDIUM-HIGH confidence)\n\n- G7 requires bonding. xDrip registers `mBondStateReceiver` (line 1155) and keeps a `pairKeeper` map of transmitterID → MAC.\n- **Stale-bond cleanup**: `unBondAllG7notCurrent()` (lines 964–1001) removes any bonded G7 whose MAC isn't the current sensor, and it's called **before every scan and every connect** (lines 572, 664). Each new G7 session is a new MAC/bond, so without cleanup dead bonds accumulate — xDrip actively prunes them. This is xDrip managing the sensor's limited pairing/slot capacity.\n- The `maxDevices=3` you observed is consistent with the ecosystem's standard G7 fix: delete Bluetooth bonds named `DXCM`/`DX01`/`DX02`, disable other collectors, and wait ~15 min for the sensor to enter \"rapid reconnect mode\" ([xDrip G7 troubleshooting](https://navid200.github.io/xDrip/docs/Dexcom/G7_Troubleshooting.html); [discussion #3813](https://github.com/NightscoutFoundation/xDrip/discussions/3813)). The recurring theme \"sensor won't accept a new connection until stale slots clear\" is a recognized real G7 behavior, not unique to your setup — which lends weight to the slot-exhaustion hypothesis.\n\n---\n\n## Bottom line for your two hypotheses\n\n| Your hypothesis | Verdict from xDrip evidence |\n|---|---|\n| \"Connect path dies while scan path still works; the fix is heavy stack recreation\" | **Phenomenon confirmed** (xDrip's `always_scan` fallback + missed-window→scan switch). **Remedy questioned**: xDrip's first-line fix is *active scan → connect-on-discovery*, not per-window central recreation. Recreation is analogous to xDrip's last-resort adapter-restart watchdog. |\n| \"Our disconnect doesn't fully release, so the sensor holds our slot\" — self-inflicted by connect-per-reading | **Strongly supported mechanism.** xDrip does *not* rely on an OS-level cancel; it writes an application-layer `DisconnectTxMessage` to the Control characteristic so the **sensor** tears down and frees the slot. If your app cancels the CB connection without sending that protocol disconnect, the `.disconnecting`-stuck signature and slot-refusal are the expected result. But connect-per-reading *itself* is not the culprit — xDrip does it too and works. **Check whether your app sends the Dexcom disconnect command before cancelling.** |\n\n**Two concrete, source-backed things to verify in your own code** (both cheap to check, neither requires trusting my inferences):\n1. Does the from-stock app write the Dexcom `DisconnectTxMessage` (control-characteristic disconnect opcode) before `cancelPeripheralConnection()`? xDrip treats this as mandatory for a clean, slot-freeing teardown.\n2. On a miss, does the app fall back to an **active scan** and connect on the received advertisement (which you've proven you can see), instead of only re-arming a passive pending connect? That is xDrip's primary recovery, and it's lighter and more reliable than recreating the central.\n\n**Confidence notes:** All `file:line` claims are read directly from master source (HIGH). Android `autoConnect` true/false semantics and their mapping to CoreBluetooth's armed-connect are HIGH for Android, MEDIUM for the cross-stack analogy. The claim that *your* app lacks the protocol disconnect is UNVERIFIED — it's a hypothesis I'm flagging, checkable in your codebase. Whether the G7 would eventually self-disconnect even without the command is unknown; xDrip sends it regardless to force a fast, acknowledged close.",
      "I have everything needed. Here is my synthesis.\n\n---\n\n# Dexcom G7 BLE Connection Model — findings\n\n## Bottom line (the central question)\n\n**After a client reads an EGV, the G7 firmware DISCONNECTS the client. It does NOT keep the client connected to push the next EGV as a notification ~5 min later.** \"Connect-per-reading\" is not a design choice your team made in isolation — it is the *only* model the G7 offers, and every reference implementation uses it. Confidence: **HIGH**, from multiple independent primary sources including the exact upstream code your app is built on.\n\nThis means the framing \"our connect-per-reading architecture is self-inflicting the problem\" is **half right in mechanism but wrong in remedy**: you cannot switch to a \"stay-connected, receive notifications\" model because the G7 doesn't support one. The genuine defect (a connection that doesn't fully release) is real, but it is a *watchOS CoreBluetooth teardown* problem, not evidence that the architecture is wrong.\n\n---\n\n## Evidence for the disconnect model\n\n### Primary source #1 — upstream G7SensorKit (the code your app wraps)\n`/Users/jeremybarnum/Downloads/Loop/March2026/LoopWorkspace-fromstock/G7SensorKit/G7SensorKit/G7CGMManager/G7BluetoothManager.swift`\n\n- Lines 236–250, verbatim comment by Pete Schwamb (Loop lead):\n  > \"Persistent connections don't seem to work with the transmitter shutoff: The OS won't re-wake the app unless it's scanning. The sleep gives the transmitter time to shut down, but keeps the app running.\"\n  \n  This is a direct statement that **the transmitter powers its radio down after the read** — a persistent connection is impossible because the peer's radio goes away. On disconnect the manager calls `scanAfterDelay()` (2s), i.e. it re-acquires each window rather than holding a link.\n- `didDisconnectPeripheral` (lines 380–406) treats `CBError.peripheralDisconnected` as **expected** (\"Ignore errors indicating the peripheral disconnected remotely, as that's expected behavior\") and classifies the normal disconnect as `wasRemoteDisconnect = true`. The **sensor** initiates the normal disconnect.\n\nAlso structurally important: upstream G7SensorKit is a **passive observer**, not an authenticator. `G7Sensor.swift` only ever *listens* to the authentication characteristic and reads `AuthChallengeRxMessage{isAuthenticated, isBonded}` (the sensor's report) — the only thing it ever *writes* is the version request (`G7PeripheralManager.swift:95`). It was designed to ride alongside the Dexcom phone app as a secondary listener. Your watch app had to add its own active J-PAKE/cert/AES authenticator (`Loop/WatchApp Extension/G7/G7Client.swift`) precisely because on-wrist there is no phone app doing the auth. Worth flagging: this is a real divergence from what upstream was built and field-hardened for.\n\n### Primary source #2 — your own watch client\n`/Users/jeremybarnum/Downloads/Loop/March2026/LoopWorkspace-fromstock/Loop/WatchApp Extension/G7/G7Client.swift`\n\nYour own code already encodes the answer as fact:\n- Line 1061: *\"The G7 disconnects after every read BY DESIGN (connect-per-reading) and only re-advertises on its fixed ~5-min EGV grid.\"*\n- Lines 1550–1551: *\"this is the EXPECTED post-read disconnect (the G7 hangs up after each reading).\"*\n- Line 1698: *\"Disconnect (the G7 will drop us in a few seconds anyway).\"*\n\n### External corroboration\n- Dexcom / Levels / general: the G6 pattern \"connect, send data, and disconnect\" **\"also applies to the G7\"**; the Bluetooth pairing legitimately reads \"not connected\" between windows.\n- Juggluco changelog (v9.0.13): *\"Added connect timer for Dexcom G7 sensors, to awake a WearOS watch to make a connection to the sensor.\"* A production **watch** implementation uses exactly your model — a timer that wakes the watch to connect for each window. This is strong external validation that connect-per-reading is correct on a watch, not a bug.\n\n---\n\n## Q1 — Is the ~5-min connectable burst a \"no client connected\" behavior?\n\n**No. The ~5-min connectable burst is the sensor's baseline duty cycle regardless of whether anyone is connected.** Confidence: **HIGH.**\n\nBecause *every* client (phone, watch, receiver) is connect-per-reading, the sensor spends almost all of each 5-min interval with its radio off and no active connections, then wakes for a ~6s connectable burst aligned to the EGV grid so all of its provisioned clients can independently reconnect, read, and drop. Seeing connectable ads every 5 min tells you the sensor is in **normal** operation — it is *not* a signal that it thinks it has lost you.\n\nThe discriminating detail (xDrip \"G7 Rapid Reconnect\" doc): when the G7 genuinely loses a client — **3 consecutive missed acknowledgments (~15 min)** — it does the opposite of going quiet: it switches to **once-a-minute** advertising (\"rapid reconnect\") for up to 12 hours, reverting to 5-min \"as soon as a handshake occurs.\" \n\nImplication for your diagnosis: if your misses were because the *sensor* had dropped/freed your slot, then during a multi-window blackout you would expect to see **1-minute** ad spacing, not 5-minute. Your \"131 window observer\" already timestamps ad sightings — measuring the **inter-advertisement interval during a blackout** is the cleanest single experiment to separate the two hypotheses. 5-min spacing during a blackout ⇒ the sensor still thinks a client is healthy and the fault is entirely client-side; 1-min spacing ⇒ the sensor considers you gone and is soliciting you (which would *exonerate* \"sensor holds our slot\" as the cause, not confirm it).\n\n---\n\n## Q2 — What does maxDevices=3 govern?\n\n**Three role-keyed display \"channels\"/slots — phone, smartwatch, receiver — provisioned as bonds, each doing its own connect-per-reading.** Confidence: **HIGH** on the count and roles; **MEDIUM** on precise bond-vs-connection semantics.\n\n- Dexcom's own Direct-to-Watch FAQ: \"up to 3 display devices… three Bluetooth channels: Smartphone, Smartwatch, Dexcom Receiver,\" and Direct-to-Watch **consumes one channel**.\n- Your own code confirms the slot model directly (`G7Client.swift:570–587`): `authEndByte` is the **slot selector** (your comment: 'xDrip \"keks\" names it `slot`'): `0x01` = concurrent/secondary slot (coexists with the phone, auth=1, your PROVEN default), `0x02` = phone/standard (collides with the real phone → auth=2/reject), `0x04` watch, `0x08` receiver. The `0xEA` device-list response returns `maxDevices` and per-record `displayType`.\n\nSo it is **not** \"3 simultaneous *persistent* connections\" (nothing is persistent) and not merely \"3 bonds you can register and forget.\" It is **3 concurrent role slots**: each is a durable bonded relationship that is only briefly *connected* during each window. Up to 3 can be connected at once during a burst; between bursts typically zero are. The `auth=2` rejection path in your code proves the sensor enforces slot/role collision — two clients contending for the *same* role slot get one rejected.\n\n---\n\n## Q3 — Does a half-open / incompletely-released connection make the sensor refuse a new one while still advertising?\n\nThis splits into two claims. Confidence: **MIXED — read carefully.**\n\n**(a) Slot-occupancy refusal is real and documented.** — Confidence: **HIGH.** xDrip's G7 guidance: if a pump/receiver/watch already holds a slot, another collector \"will not be able to connect\"; the fix is to forget the DXCM/DX01/DX02 **bond** and **\"wait 15 minutes for G7 to enter rapid reconnect.\"** (xDrip discussion #4573: \"connect standalone device while xdrip+ session is active fails.\") So the sensor *does* refuse connections when its role slots are taken, and stale occupants clear on a timeout consistent with the ~15-min/3-missed-ack rapid-reconnect timer.\n\n**(b) That YOUR OWN sub-5-minute half-open link is what blocks YOUR next connect** — Confidence: **LOW / not supported by external evidence; more likely the same root cause as the \"poisoned central,\" viewed from the sensor side.** Reasoning:\n\n1. The documented slot-refusal timeout is ~15 min (rapid-reconnect grid), an order of magnitude longer than your 5-min miss cadence. A slot the sensor genuinely believed occupied would not free itself in time to explain a *next-window* recovery, yet your logs show recovery within one or two windows after a central-recreate.\n2. Your \"stuck in `.disconnecting` for >5s\" signature and \"cancelled connects wedged forever, cured by reboot/central-recreate\" (`G7Client.swift:686–693, 1210–1218`) is a **well-known watchOS/iOS CoreBluetooth pathology** on the *client* side. `retrievePeripherals` returning the same cached wedged object (\"fresh handle=false always,\" your line 1216) is the fingerprint of a stack-local wedge, not a peer decision.\n3. **The strongest discriminator:** you cure it by recreating the `CBCentralManager` — a purely **client-side** object. The sensor has no visibility into which central instance you use. If a purely local reset restores connectivity, the blocking state lived in your stack.\n\nThe important caveat that keeps (b) from being fully dismissed — and that means the two hypotheses are probably **one root cause**: a `.disconnecting`-stuck peripheral means **the OS never completed the link teardown**, so the sensor's link layer may *also* still see that connection half-up until its own supervision timeout. Destroying the central forces the OS to finally abandon the ACL link (emitting/allowing the LL terminate), which frees **both** your stack **and** the sensor's slot. So \"poisoned central\" and \"our connection didn't fully release\" are the **same defect seen from two ends: an incomplete teardown in the watchOS BLE stack.**\n\n### The mechanism that ties it all together (inference, HIGH internal consistency)\nYour success path proactively calls `cancelPeripheralConnection` in `finishAttempt` (`G7Client.swift:1043`) at essentially the same instant the sensor powers its radio off and drops you (\"the G7 will drop us in a few seconds anyway,\" line 1698). That is a **race**: if you issue the disconnect while the peer radio is already gone, the client-side LL_TERMINATE handshake can't complete → `CBPeripheral` stuck in `.disconnecting` → the next armed `connect()` against that wedged object is silently swallowed. This precisely predicts your three field signatures:\n- **First read of a fresh session always succeeds** — clean stack, no prior link to tear down.\n- **Immediately following windows miss** — the teardown race wedged the peripheral object.\n- **Passive scan still sees the connectable ad during the miss** — the sensor is fine and advertising normally; only your connect path is dead.\n- **Central-recreate cures it, then degrades** — a fresh stack until the race re-wedges it.\n\n---\n\n## What this says about your suspect conclusion\n\n- \"The connect path is dead while the scan path works; recreating the central cures it\" — **supported.** This is a real watchOS CoreBluetooth wedge.\n- \"Connect-per-reading is self-inflicting the problem\" — **wrong as stated.** Connect-per-reading is mandatory; the G7 has no persistent-notification mode (upstream's \"transmitter shutoff,\" universal connect/send/disconnect, Juggluco's WearOS connect-timer all agree). Abandoning connect-per-reading is not an available fix.\n- \"Our own connection doesn't fully release, so the sensor still counts our slot and refuses us\" — **the teardown-doesn't-complete part is likely true; the sensor-refuses-us part is probably not the operative cause** (client-side reset fixes it; sensor advertises normal 5-min, not rapid-reconnect). Most likely both descriptions are the same incomplete-teardown defect.\n\nTwo concrete, non-code diagnostics to settle it definitively (flagged as suggestions, not implemented): (1) log the **inter-ad interval during a blackout** via your 131 observer — 5-min vs 1-min cleanly separates \"client wedge\" from \"sensor freed our slot\"; (2) test **not** issuing your own `cancelPeripheralConnection` on the success path and letting the sensor's own disconnect land — if the `.disconnecting`-stuck rate drops, the teardown race is confirmed as the mechanism.\n\n---\n\n## Sources\n\nPrimary code (absolute paths):\n- `/Users/jeremybarnum/Downloads/Loop/March2026/LoopWorkspace-fromstock/G7SensorKit/G7SensorKit/G7CGMManager/G7BluetoothManager.swift` (lines 236–250 \"transmitter shutoff\"; 380–406 remote-disconnect-expected)\n- `.../G7SensorKit/G7SensorKit/G7CGMManager/G7Sensor.swift` (passive observer; auth listen-only)\n- `.../G7SensorKit/G7SensorKit/G7CGMManager/G7PeripheralManager.swift`; `.../Messages/AuthChallengeRxMessage.swift`, `.../Messages/G7Opcode.swift`\n- `/Users/jeremybarnum/Downloads/Loop/March2026/LoopWorkspace-fromstock/Loop/WatchApp Extension/G7/G7Client.swift` (lines 570–587 slot selector; 686–693 self-heal; 1043 proactive cancel; 1061, 1210–1218, 1550, 1698 disconnect-by-design; 1846–1856 device-list/maxDevices)\n\nExternal:\n- [Dexcom G7 Direct-to-Watch — device count/channels](https://www.dexcom.com/en-ca/faqs/g7/how-many-devices-with-direct-to-watch)\n- [Dexcom Provider — D2W + AID coexistence](https://provider.dexcom.com/if-my-patient-uses-direct-watch-feature-can-they-still-connect-their-aid-system)\n- [xDrip — G7 Rapid Reconnect (5-min vs 1-min advertising)](https://navid200.github.io/xDrip/docs/Dexcom/G7OnceAMinuteScan.html)\n- [xDrip — G7/One+/Stelo Troubleshooting (slot occupancy, forget-bond, 15-min wait)](https://navid200.github.io/xDrip/docs/Dexcom/G7_Troubleshooting.html)\n- [xDrip — Dexcom device limitations (\"only one app can collect\"; connect xDrip before receiver)](https://navid200.github.io/xDrip/docs/Receiver-or-tslim-and-xDrip.html)\n- [xDrip discussion #4573 — standalone device fails while xDrip+ session active](https://github.com/NightscoutFoundation/xDrip/discussions/4573)\n- [Juggluco changelog (WearOS connect-timer; keep-connected-for-backfill)](https://www.juggluco.nl/Juggluco/changes.html)\n- [AndroidAPS — Dexcom G7 and ONE+](https://androidaps.readthedocs.io/en/latest/CompatibleCgms/DexcomG7.html)\n- [Levels — Dexcom G6/G7 connect-send-disconnect behavior](https://support.levels.com/article/250-dexcom-troubleshooting-signal-loss-or-poor-signal)",
      "I have enough primary and secondary evidence to answer all five questions rigorously. Here are my findings.\n\n---\n\n# CoreBluetooth first principles — findings for the G7 connect-per-reading problem\n\n**Confidence legend:** [DOC] = Apple documentation or Apple-staff statement; [REP] = corroborated developer reports; [INF] = my inference from link-layer / CB semantics, flagged as such.\n\n## Bottom line up front\n\nTwo of your \"facts\" are, on the CoreBluetooth evidence, load-bearing in a direction that undercuts the \"poisoned central\" conclusion:\n\n1. **watchOS caps BLE connections at 2 per app, and this is Apple-confirmed, not folklore.** You are also holding an Omnipod (OmniBLE) connection from the same watch app. A G7 connection that does not fully release leaves you at the 2-slot ceiling, and the *next* `connect()` then silently never fires — which is exactly your signature (advertisements received on a scan, armed connect never completes, no error). This is a documented, non-folklore mechanism that your current \"half-poisoned central\" story does not account for.\n\n2. **`cancelPeripheralConnection` is documented to *not* guarantee the link drops, is nonblocking, and in iOS 17+ sometimes produces no disconnect at all.** So your competing hypothesis — \"our connection may not fully release; the sensor/OS still counts us as occupying a slot\" — is supported by Apple's own documented semantics, not just by the `.disconnecting`-stuck log line.\n\nNet: the evidence favors **connection-not-released / slot-exhaustion** over **radio/scan-path poisoning**. Recreating the central \"cures\" it not necessarily because the radio was poisoned, but because a fresh central force-drops your app's local connection bookkeeping, re-discovers the peripheral as a *new* `CBPeripheral` instance, and frees the slot.\n\n---\n\n## Q1. What makes a \"pending\" connect fire with no active scan? Does hearing an advertisement in a concurrent scan imply the connect should have fired?\n\n**What is documented [DOC/REP]:**\n- `connect(_:options:)` **never times out**. Apple/CB behavior: \"Calls to connect never time out… call connect, and you'll get notified when it comes back in range, however long that may take.\" A pending connect \"waits indefinitely (unless Bluetooth is interrupted or the app is manually killed).\" (Punch Through guide; corroborated by Splinter and arkulpa write-ups.)\n- You **must retain a strong reference** to the `CBPeripheral` for the whole pending period or \"iOS will cancel the connection.\" (Same sources; Apple forums thread 84098 quotes the API-misuse warning \"Cancelling connection for unused peripheral… Did you forget to keep a reference to it?\")\n\n**How it actually fires [INF, grounded in BLE LL spec + the Microchip LL roles doc + US Patent 9,706,496 \"Concurrent BLE scanning and initiation\"]:**\n- A pending connect puts the *controller* into the **initiating** state for that target (typically via the controller's accept/white list). Initiating is a **distinct link-layer procedure** from scanning: the initiator must (a) receive a *connectable* advertising PDU from that specific device on an advertising channel, then (b) transmit a `CONNECT_IND`/`CONNECT_REQ` exactly T_IFS (150 µs) later on the same channel, and (c) have the advertiser accept it. Only then does `didConnect` fire.\n- Your app-level **scan** is a *receive-only* procedure that delivers ADV reports to `didDiscover`. It does **not** transmit `CONNECT_IND`. The controller **time-slices** scanning vs. initiating (that is the entire subject of the \"concurrent scanning and initiation\" patent — they are separate activities that must be scheduled).\n\n**Therefore — direct answer:** **No.** Receiving the sensor's connectable advertisement in a concurrent diagnostic scan does **not** imply the pending connect should have connected. Scan-reception and connect-establishment are **independent paths**:\n- The scan proves your radio's RX and your central's callback path are alive and the sensor is emitting connectable adverts in the window.\n- The connect additionally requires the *initiator* to be armed for that peripheral, to win its own RX slot on the adv event, to TX the `CONNECT_IND`, and for the sensor to *accept* it. Any of those can fail invisibly while the scanner still hears the adverts.\n- **Corollary that actually argues against your conclusion:** if the *same* `CBCentralManager` is delivering scan callbacks during the miss, the central and radio RX are demonstrably *not* dead. That points at the **specific pending-connect/initiator or the peripheral object being stale**, not a poisoned central. Confidence: **high** on independence; **medium-high** on the corollary.\n\n---\n\n## Q2. Known failure modes where an outstanding `connect()` never completes despite the peripheral advertising connectable — causes and cures short of recreating the central\n\n**Documented / corroborated failure modes [REP, multiple Apple forum threads]:**\n- **Since iOS 11:** central \"gets stuck in connecting state, having never gotten a `didConnect`… once stuck, persists indefinitely.\" Advertisements still visible; other apps (nRF Connect) connect fine. (Threads 94011, 742497.)\n- **iOS 17 root cause identified by a developer [REP]:** with state restoration, CoreBluetooth can hand back a **new `CBPeripheral` instance with the *same* `identifier` but a different object and a stale state** (e.g. one instance `disconnected`, the retained one `connecting`). Code that matches peripherals by `identifier` (not object identity) then re-arms `connect()` on a **stale peripheral object → the connect never fires**. (Thread 742497.) **This is the single most checkable mechanism for your symptom and it would be \"cured\" by a central recreate** (forces fresh discovery → fresh instance).\n- **State-restoration mis-teardown (iOS 11+) [REP]:** if a connection isn't cancelled properly before releasing the central, \"any future CBCentralManagers with the same restoration ID will not be able to connect,\" and `state` can come back `.unknown`/`.poweredOff`. (Thread 84098.)\n- **watchOS is explicitly different [DOC, Apple DTS]:** on watchOS, `connect()` fires but `didConnect`/`didFailToConnect` **never come and the peripheral stays in `.connecting`** — Apple DTS engineer Argun Tekant: *\"CoreBluetooth on watchOS does not work the same way it does on iOS. There are more limitations, and things work differently. So, the code you have that works on iOS will not work on watchOS as-is.\"* (Thread 774914.) This is directly your platform.\n\n**Cures reported, short of recreating the central [REP]:**\n- The uncomfortable finding: for the *truly* wedged state, developers report that **`cancelPeripheralConnection` + retry does NOT cure it, toggling the peripheral's adv address does NOT cure it, and even a new `CBCentralManager` only sometimes cures it** — the reliable cures were **hard Bluetooth toggle, app force-quit, or device reboot** (threads 94011, 84098). Note this *weakens* the \"recreate central reliably cures it\" story: recreate is reported as ~50% effective, sometimes coming up in `.unknown`.\n- The lighter, *targeted* fix that is documented: **eliminate the second central manager** — one reporter fixed the stuck-state by using a **single** `CBCentralManager` for both discovery and connection rather than two (thread 742497). Relevant if your diagnostic scan and your armed connect are on different centrals.\n\nConfidence: **high** that these failure modes exist and match your symptom; **medium** on which specific one you're hitting (the iOS17 stale-instance and the watchOS-difference are the top two candidates).\n\n---\n\n## Q3. What does a `CBPeripheral` stuck in `.disconnecting` (state 3) indicate? Can it block subsequent connects? Does `cancelPeripheralConnection` always complete?\n\n**`.disconnecting` meaning [REP]:** the app has requested teardown (or the OS is tearing down) but `didDisconnectPeripheral` has not yet been delivered. In the pathological reports it **toggles only between `.connecting` and `.disconnecting` indefinitely** — the peripheral never returns to `.disconnected`, so the app can never legitimately re-arm a clean connect. (Thread 742497.) A `.disconnecting` state persisting **>5 s**, as in your logs, is well outside normal teardown (which is sub-second) and matches this wedged pattern.\n\n**Can it block subsequent connects to the same peripheral? [DOC + REP + INF] — yes, in two distinct ways:**\n- *App/OS side:* while the peripheral is `.connecting`/`.disconnecting`, that peripheral is not `.disconnected`, and the OS still holds a local connection reference for it. On watchOS this **counts against the per-app 2-connection budget** (Q5). A new armed connect can therefore be refused/ignored *silently* (no `didFailToConnect`, because the connect never leaves the pending initiator state). [INF, medium-high]\n- *Sensor side:* `cancelPeripheralConnection` **only cancels your app's *local* connection**; Apple: *\"Because other apps may still have a connection to the peripheral, canceling a local connection does not guarantee that the underlying physical link is immediately disconnected.\"* If the LL link is not actually torn down over the air, the **G7 still counts you against its `maxDevices=3`** and keeps advertising while refusing your re-connect — exactly your competing hypothesis. [DOC, high]\n\n**Does `cancelPeripheralConnection` always complete? — No. [DOC + REP]:**\n- Apple documents it as **nonblocking**; pending commands \"may or may not finish executing,\" and the physical link may not drop.\n- In **iOS 17** there are corroborated reports that `cancelPeripheralConnection` **produces no disconnect and no error** — the peripheral simply stays connected. So you cannot assume `didDisconnectPeripheral` will arrive. Your \">5 s in `.disconnecting`\" is consistent with a cancel that is not completing. [REP, medium-high]\n\nImplication: a design that assumes disconnect always completes cleanly before the next window is **relying on an undocumented guarantee**. This is a genuine flaw in the connect-per-reading loop, independent of any radio poisoning.\n\n---\n\n## Q4. Does recreating the `CBCentralManager` release connections the OS still considers \"ours\"? Is there a lighter cure?\n\n**What recreate does [DOC semantics + INF]:**\n- When a `CBCentralManager` deallocates, its **local** connections are dropped and its peripheral objects are released. That frees *your app's* local references and, on watchOS, frees your per-app connection slots. So recreate **does** release \"ours\" from your app's point of view. [INF from CB ownership model, medium-high]\n- **But** the *system-level* physical link is reference-counted across the whole system, not owned solely by your central instance. Apple: canceling a local connection \"does not guarantee that the underlying physical link is immediately disconnected.\" So recreating the central does **not** guarantee the over-the-air link to the G7 is torn down instantly — the sensor-side slot may linger briefly. [DOC, high] This is consistent with your \"freshly-created central often works then degrades again within a window or two\": the fresh central re-arms cleanly, but if teardown still isn't clean, you re-accumulate the stuck state.\n\n**Lighter cures than full recreate [DOC + REP]:**\n- **`retrieveConnectedPeripherals(withServices:)`** — Apple's *documented* reconnection path: the G7 may be **already connected to the system** (from your not-fully-released prior link, or another app). You then must **still call `connect()` locally** to re-adopt it: *\"Even though the device is already connected to the system, you must still connect it locally to your app.\"* Adopting the existing system connection is cheaper and avoids re-running discovery. Worth trying *before* recreating the central. [DOC, high]\n- **`retrievePeripherals(withIdentifiers:)`** then `connect()` — reconnect to a known peripheral without scanning; but note it hands you a `CBPeripheral` that may carry stale state (Q2 iOS-17 mechanism), so treat the returned object as authoritative and don't re-use an old retained instance. [DOC + REP]\n- **Explicit `cancelPeripheralConnection` on every `retrieveConnectedPeripherals` result at session start** (a \"cleanup loop\") — documented pattern for clearing stale system connections before arming a fresh connect. [DOC/REP, medium]\n- **Collapse to a single central** for both scan and connect (Q2) — reported to fix the stuck-state in at least one case. [REP, medium]\n\nCaveat [REP]: for the *fully* wedged state, none of the lighter cures reliably work — only BT toggle / reboot did. So the lighter cures are worth trying first, but budget for the possibility that the state is unrecoverable within-process.\n\n---\n\n## Q5. Per-app / per-central limit on outstanding connects or connected peripherals on watchOS/iOS that could explain session-long degradation?\n\n**watchOS [DOC — Apple Staff, this is the headline finding]:**\n- *\"On watchOS, an app can connect to 2 BLE peripherals at the same time. The limit is per app.\"* (Apple-staff accepted answer, thread 738861.) visionOS is likewise 2 (thread 812353). This is a **hard, low, per-app ceiling** — far below iOS.\n- The thread does **not** clarify whether a peripheral stuck in `.connecting`/`.disconnecting` counts against the 2. But from the CB ownership model, a peripheral that is not `.disconnected` is still a live local connection, so it **almost certainly consumes a slot** until teardown completes. [INF, medium-high]\n\n**iOS [DOC/REP]:** no fixed published number; the practical simultaneous-connection limit \"differs based on device type, model, iOS version and current state… not published and can change.\" Apple's guidance is to detect gracefully when you can't add more. So iOS won't explain your degradation the way watchOS will.\n\n**Why this plausibly explains session-long degradation [INF, medium-high — strongest reframing of your problem]:**\n- If your watch app holds the **Omnipod** connection, that's **1 of 2** already.\n- The **first G7 read of a fresh session succeeds** because both slots are clean.\n- The connect-per-reading disconnect **doesn't fully release** (Q3: nonblocking cancel, iOS-17 no-op cancel, `.disconnecting`-stuck). The half-released G7 lingers as slot 2.\n- The **next** window's armed `connect()` now has **no free slot**, so the OS never launches the initiator — you still *scan*-hear the sensor (RX unaffected) but the connect **silently never completes and never errors** (consistent with the watchOS \"connect never fires, stays `.connecting`\" behavior, and with connect having no timeout).\n- **Recreating the central force-frees the leaked slot**, so subsequent windows catch again — \"for a while,\" until the leak re-accumulates. This matches every field observation you listed **without** requiring a poisoned scan-vs-connect radio split.\n\nThis is the mechanism I'd rank first, precisely because it is the one that would show your current conclusion to be wrong: it explains all the same data, is built entirely from Apple-documented constraints, and predicts a different fix (guarantee real teardown / manage the 2-slot budget / adopt via `retrieveConnectedPeripherals`) than \"recreate the central.\"\n\n---\n\n## How this maps onto your two hypotheses\n\n- **Your suspect conclusion (\"central half-poisoned: scan works, connect dead, recreate cures\"):** partly supported as a *phenomenon* (iOS 11/17 stuck-connect reports are real), but the \"recreate reliably cures\" claim is **weaker than you think** (reported ~50%, sometimes comes back `.unknown`; hard cure is BT toggle/reboot). And the fact that the *same* central still delivers scan callbacks during a miss argues the central/radio are **not** poisoned. Confidence this is the primary cause: **low-to-medium**.\n- **The competing hypothesis (\"our connection doesn't release; slot stays occupied\"):** **better supported by documented CoreBluetooth semantics** — nonblocking cancel that doesn't guarantee link teardown, iOS-17 cancels that no-op, `.disconnecting`-stuck, the watchOS **2-per-app** ceiling, and system-level reference-counted links the sensor (`maxDevices=3`) also tracks. Confidence this is at least a major contributor: **medium-high**.\n- **A third, more concrete mechanism to rule out first:** the **iOS-17 stale-`CBPeripheral`-instance** bug — if you re-arm `connect()` on a peripheral matched by `identifier` rather than the freshest object CoreBluetooth handed you, the connect targets a dead object and never fires; a central recreate masks it. Cheap to check in your code and logs (compare object pointer, not just `identifier`, across windows). Confidence it's worth checking: **high**.\n\nTwo things I could **not** find documented (flagging honestly): (1) any Apple statement on whether a `.connecting`/`.disconnecting` peripheral counts against the watchOS 2-slot budget — I inferred yes; (2) any Apple confirmation that a passive scan and an armed connect on the *same* central use fully independent controller scheduling on watchOS specifically — the independence is solid at the BLE-spec level but I have no watchOS-specific primary source.\n\n---\n\n## Sources\n\n- Apple DTS on watchOS CB being fundamentally different (connect never fires, stays `.connecting`): https://developer.apple.com/forums/thread/774914\n- Apple Staff: watchOS limit is **2 BLE peripherals per app**: https://developer.apple.com/forums/thread/738861\n- visionOS limited to 2 connections (corroboration): https://developer.apple.com/forums/thread/812353\n- `CBPeripheral` stuck `.connecting`/`.disconnecting`, iOS 17 stale-instance-with-same-identifier root cause, other apps connect fine, adverts still visible: https://developer.apple.com/forums/thread/742497\n- iOS 11 stuck-connect; cancel+retry / adv-address toggle / reference check all fail; only hard BT toggle or reboot cures: https://developer.apple.com/forums/thread/94011\n- iOS 11 state-restoration teardown bug poisons future centrals with same restoration ID; API-misuse \"keep a reference\" warning; Apple engineer on stricter iOS 11 object lifecycle: https://developer.apple.com/forums/thread/84098\n- `cancelPeripheralConnection` is nonblocking and does not guarantee physical-link disconnect: https://developer.apple.com/documentation/corebluetooth/cbcentralmanager/cancelperipheralconnection(_:)\n- Apple archive — reconnecting via `retrieveConnectedPeripheralsWithServices:` and \"you must still connect it locally\": https://developer.apple.com/library/archive/documentation/NetworkingInternetWeb/Conceptual/CoreBluetooth_concepts/BestPracticesForInteractingWithARemotePeripheralDevice/BestPracticesForInteractingWithARemotePeripheralDevice.html\n- Punch Through CB guide — pending connect \"waits indefinitely,\" no timeout, disconnect semantics: https://punchthrough.com/core-bluetooth-guide/\n- \"connect never times out\" corroboration: https://www.splinter.com.au/2019/05/18/ios-swift-bluetooth-le/ and https://medium.com/arkulpa/ios-stay-connected-to-an-external-ble-device-as-much-as-possible-699d434846d2\n- BLE link-layer initiator vs. scanner states / `CONNECT_REQ`: https://developerhelp.microchip.com/xwiki/bin/view/applications/ble/introduction/bluetooth-architecture/bluetooth-controller-layer/bluetooth-link-layer/RolesStates/\n- Scanning and initiating are separate, scheduled activities (patent): https://image-ppubs.uspto.gov/dirsearch-public/print/downloadPdf/9706496\n- iOS-17 `cancelPeripheralConnection` no-op reports: https://discussions.apple.com/thread/255658068",
      "I have what I need, including the decisive primary source: Loop's own G7 BLE manager source. Here is the assessment.\n\n---\n\n# Skeptical assessment: reverse-engineering \"shortcuts\" for the watchOS G7 connect problem\n\n## Bottom line up front\n\nBoth shortcuts are aimed at the wrong target. They would tell you **how to talk to a G7** — a problem you have already solved (your first read of every session succeeds, and the 7-8s handshake \"succeeds reliably when a connection is established\"). Your actual problem is a **connection-lifecycle / OS-radio-scheduling / possible sensor-slot-occupancy** problem. Neither the Dexcom binary nor the open protocol code is primarily about that.\n\n- **Idea 1 (reverse-engineer the Dexcom G7 iOS app binary): do not do this.** High effort, low-to-zero transferable value, three independent walls (decryption, legibility, OS/architecture mismatch), and it observes the wrong operating system solving a different problem.\n- **Idea 2 (do the open sources dominate the binary on value?): yes, overwhelmingly** — but the highest-value open source is not the one you'd expect, and reading it *changes the diagnosis*. Loop's own `G7SensorKit` already made the **opposite** design choices on the exact two axes you're fighting, and documents *why* in a code comment.\n\nThe real recommendation is at the end: point \"reverse-engineering\" effort at `G7SensorKit/G7CGMManager/G7BluetoothManager.swift`, not at a Mach-O.\n\n---\n\n## Idea 1 — Reverse-engineering the Dexcom G7 iOS app binary\n\nThree walls, each independently sufficient to kill it.\n\n### Wall 1 — You cannot statically read it without first defeating FairPlay, which in 2026 means a jailbroken device you probably don't have\nApp Store binaries ship with the main executable **FairPlay-encrypted**: the Mach-O carries `LC_ENCRYPTION_INFO_64` with `cryptid = 1`, and the `__TEXT/__text` bytes inside the crypt range are ciphertext on disk. Hopper / Ghidra / IDA fed the App Store IPA see encrypted garbage over exactly the code you care about. To get plaintext you must dump the **decrypted pages from memory on a device that can run the app with the FairPlay layer stripped** — historically Clutch/`frida-ios-dump`/`bagbak`/`flexdecrypt`, all of which need a **jailbroken (or rootless-jailbroken) iPhone on a jailbreakable iOS version**. On current hardware/iOS that is scarce-to-nonexistent, and the alternative (grabbing a pre-decrypted IPA from a warez \"decrypted IPA\" site) is a legal/medical-device minefield and version-roulette. This prerequisite alone is a multi-day yak-shave before you read a single instruction.\n\n### Wall 2 — Even decrypted, the thing that matters is barely legible, and it's a *public* API you can already see used\nDecrypted, it's an ARM64 Mach-O. Swift retains type metadata, reflection metadata, and mangled (demanglable) symbol names, and ObjC selectors/class names survive in `__objc_*` — so `class-dump-swift` gives you *names*. It does **not** give you logic. Hex-Rays/Ghidra pseudocode of **Swift** is notoriously the worst decompiler target there is: ARC retain/release noise, value-witness tables, generic specialization, closures lowered to partial-applies. Reconstructing a timing-sensitive connection state machine from that is brutal.\n\nAnd here's the part that makes the whole exercise pointless: **CoreBluetooth is a public framework.** The Dexcom app calls the same `scanForPeripherals`, `connect(_:options:)`, `cancelPeripheralConnection`, `registerForConnectionEvents` that you call. There is no secret private-API connection path to discover — if there were, you'd see it as `objc_msgSend` into a public dylib. The only \"insight\" a static decompile yields is *which* public calls, in what order, with what option dictionaries — and the far cheaper way to get that is **Frida hooks on `CBCentralManager`/`CBPeripheral` selectors** logging the live call sequence. But that *still* needs the jailbroken device, and it lands you at Wall 3.\n\n### Wall 3 — It's the wrong OS solving a different problem (this is the fatal one)\nEven a flawless behavioral trace of the Dexcom app is worthless for your bug, for structural reasons:\n\n1. **iOS ≠ watchOS.** Your symptom — a `CBCentralManager` whose *scan* path works during the window (RSSI −78…−86, connectable=YES) while the *armed connect* never completes, cured by recreating the central — is a **watchOS radio-scheduling / central-lifecycle** phenomenon. iOS CoreBluetooth has different background execution, radio arbitration, and power management. The iOS app cannot exhibit your failure and therefore contains no code that fixes it.\n2. **Primary collector ≠ connect-per-reading.** The Dexcom app is the sensor's **primary, near-persistent** collector with an established bond; it is not arming a fresh pending-connect 45s before each 5-min grid slot and tearing down after one EGV. Its connection strategy is answering a question you're not asking.\n3. **The relevant behavior lives *below* the app anyway** — in the sensor firmware's slot/session expectations and in Apple's BLE stack — neither of which is in the app binary.\n\n### Idea 1 verdict\n**Effort:** high (jailbreak sourcing → decrypt → Swift disasm/Frida → interpret), realistically days-to-weeks with specialized prerequisites. **Value:** ~zero transferable to a watchOS connect-per-reading standalone collector. **Plus** a real DMCA/EULA/medical-device legal shadow. **Do not pursue.** The one scenario where binary RE is ever justified — an *undocumented protocol* — does not apply here, because the protocol is already open (below).\n\n---\n\n## Idea 2 — Do the already-open sources dominate the binary on value?\n\n**Yes, and it isn't close** — with an important refinement about *which* source and *what it actually gives you*.\n\n### The protocol is fully solved in the open, across the ecosystem\n- **xDrip4iOS** (`JohanDegraeve/xdripswift`, Swift, GPLv3) has G7 direct-BLE support, and its changelog explicitly describes fixing G7 connection via a **\"strict auth-first sequence and removal of forced disconnects, allowing the connection to complete normally\"** — i.e. the community already hit, and fixed, an auth/disconnect-ordering pathology.\n- **Loop `G7SensorKit`** (Swift 99%, readable) — same language and framework family you already use.\n- **DiaBLE** (`gui-dos/DiaBLE`) documents the G7 opcode sequence (auth stages `02…07`, backfill `0x59`) and confirms Dexcom **\"wraps the mbedtls framework to implement the J-PAKE authentication protocol,\"** that there is *no clean open-source Swift J-PAKE* (the working implementations are in Android xDrip via mbedtls), and — critically for you — that there are **auth commands \"that would cause sensors to disconnect.\"** The sensor-side disconnect-on-auth behavior is a *known, documented* phenomenon, not a mystery in a binary.\n- DiaBLE also confirms **CoreBluetooth reference-counts connections**, so multiple apps can hold the same peripheral concurrently — directly relevant to your slot hypothesis (below).\n\nA binary can, by definition, reveal nothing about the protocol that these already give you in source form with names, comments, and history. So on protocol, open source doesn't just dominate — it's a total shutout.\n\n### The refinement that actually matters: read Loop's own BLE manager, because it contradicts your architecture\nThis is the highest-value finding in this whole investigation, and it's free, in Swift, in the repo you already treat as canonical.\n\n`G7SensorKit/G7CGMManager/G7BluetoothManager.swift` does **not** arm a pending `connect()` with no scan. On every disconnect it waits, then **scans**, and only calls `connect()` upon *discovering* the advertisement (`scanAfterDelay()` → `scanForPeripheral()` → `handleDiscoveredPeripheral` → `centralManager.connect`). And it says why, verbatim:\n\n> \"Persistent connections don't seem to work with the transmitter shutoff: The OS won't re-wake the app unless it's scanning. The sleep gives the transmitter time to shut down, but keeps the app running.\"\n\nNote the exact mechanics: `didDisconnectPeripheral` → `scanAfterDelay()` → `Thread.sleep(forTimeInterval: 2)` → scan. Loop deliberately **sleeps ~2s after disconnect to let the transmitter tear the session down**, then re-engages via **active scan**, not an armed connect.\n\nYour architecture does the opposite on both axes that define your bug:\n- You **arm a pending connect with no active scan**; Loop **scans and connects on discovery**. Your own field data says the **scan path is exactly the one that still works on a miss**. Loop's reference implementation rides the working path; yours rides the dead one.\n- You **tear down and immediately re-arm**; Loop **inserts a deliberate post-disconnect delay to let the sensor shut down** — which is precisely the remedy the self-inflicted-slot-occupancy hypothesis (maxDevices=3, `.disconnecting`-stuck) predicts you need.\n\nThat single file is worth more than any conceivable output of decompiling Dexcom. It's the actual \"reverse-engineered connection handling insight\" you were hoping the binary held — except it's an authored source comment from the people who shipped it.\n\nOne caveat on coverage: `G7SensorKit` is documented as **\"Requires use of official G7 app,\"** and its own code treats a no-auth disconnect as \"likely that G7 app stopped this session.\" So Loop connects alongside a sensor the official app commissioned; it is **not** doing standalone J-PAKE pairing the way your watch app is. For the *pairing/auth* path, xDrip (iOS direct mode, and Android xDrip/Juggluco for the mbedtls J-PAKE) are the closer references. So: protocol fully open, but **split across projects**, and — the honest gap — **no existing open implementation does connect-per-reading, standalone, on watchOS, inside a workout session.** That novel combination is genuinely unsolved anywhere, which is exactly why neither shortcut hands you the answer.\n\n---\n\n## Where reverse-engineering effort should and should not go\n\n**Should NOT:**\n- Decrypting/decompiling the Dexcom G7 iOS binary. Wrong OS, wrong connection model, gated behind a jailbreak you likely can't source, illegible Swift output, and it can't contain protocol facts that aren't already open. Kill it.\n\n**SHOULD (in priority order):**\n1. **Read `G7SensorKit/G7CGMManager/G7BluetoothManager.swift` and `G7Sensor.swift` line-by-line** and diff their connection *lifecycle* against yours — specifically the scan-to-discover-then-connect model and the deliberate post-disconnect delay. This directly tests your two live hypotheses and points at the fix. (`https://github.com/LoopKit/G7SensorKit`)\n2. **Read xDrip4iOS's G7 transmitter class** for the auth-first sequencing and its \"removal of forced disconnects\" change — the community's documented cure for the auth/disconnect pathology your `.disconnecting`-stuck signature resembles. (`https://github.com/JohanDegraeve/xdripswift`)\n3. **Mine the DiaBLE discussion thread** for the opcode-level session/teardown semantics and the reference-counting note, to reason about whether *your own* un-released connection (not a poisoned central) is occupying one of the sensor's 3 slots. (`https://github.com/gui-dos/DiaBLE/discussions/17`)\n4. If you still want *behavioral* ground truth about a working collector, **sniff BLE over-the-air** (a passive BLE sniffer capturing the official app ↔ sensor exchange) rather than reversing the app statically — it's lawful, OS-agnostic at the link layer, and captures real timing. But note it observes the *primary-collector* pattern, so treat it as protocol/timing reference, not as a watchOS lifecycle answer.\n\nThe meta-point to carry back: you keep looking for *\"how to talk to the sensor,\"* but you already talk to it fine. Spend the reverse-engineering budget on **lifecycle and radio-scheduling behavior in the open Swift you can actually read**, where Loop's maintainers have already left you a signed confession that your two core architectural choices — armed-connect-without-scan, and immediate re-arm without a shutoff delay — are the ones they specifically avoided.\n\n## Local artifacts\nFetched G7SensorKit sources for the quotes above are at `/tmp/g7bt.swift` (G7BluetoothManager, 449 lines) and `/tmp/g7sensor.swift` (G7Sensor, 353 lines).\n\n## Sources\n- Loop G7SensorKit — https://github.com/LoopKit/G7SensorKit (file: `G7SensorKit/G7CGMManager/G7BluetoothManager.swift`, `G7Sensor.swift`)\n- xDrip4iOS — https://github.com/JohanDegraeve/xdripswift\n- DiaBLE \"G7 without Dexcom app / Authentication\" — https://github.com/gui-dos/DiaBLE/discussions/17\n- Apple `cancelPeripheralConnection(_:)` — https://developer.apple.com/documentation/corebluetooth/cbcentralmanager/cancelperipheralconnection(_:)"
    ]
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Research"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Adversarial"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Synthesize"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "xdrip4ios",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "ac7849e4e1b32677e",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1784608018895,
      "queuedAt": 1784608010959,
      "attempt": 1,
      "lastToolName": "WebFetch",
      "lastToolSummary": "Does xDrip4iOS for Dexcom G7 / ONE+ / Stelo require the off…",
      "promptPreview": "CONTEXT — a from-stock Loop watchOS app reads a Dexcom G7 CGM DIRECTLY over BLE from an Apple Watch (no phone in the loop), to closed-loop dose an Omnipod the watch borrowed. This is novel on watchOS, but the SENSOR, its BLE protocol, and the crypto are identical to what xDrip/Juggluco/Loop-G7SensorKit already implement on iOS/Android.\n\nHARD FACTS from field testing (treat as ground truth):\n- Arch…",
      "lastProgressAt": 1784608307149,
      "tokens": 91454,
      "toolCalls": 13,
      "durationMs": 288254,
      "resultPreview": "I now have decisive evidence from the actual source plus the official docs. Here are my findings.\n\n---\n\n# How xDrip4iOS reads the Dexcom G7 / ONE+ / Stelo over BLE\n\n**Sources (all fetched and read directly):**\n- `xDrip/BluetoothTransmitter/CGM/Dexcom/G7/CGMG7Transmitter.swift` (722 lines) — the G7 subclass\n- `xDrip/BluetoothTransmitter/Generic/BluetoothTransmitter.swift` (956 lines) — the CoreBlue…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "juggluco-wear",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "adfe7594d2bbf4b94",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1784608014001,
      "queuedAt": 1784608010959,
      "attempt": 1,
      "lastToolName": "WebFetch",
      "lastToolSummary": "Extract anything about WearOS watches connecting directly t…",
      "promptPreview": "CONTEXT — a from-stock Loop watchOS app reads a Dexcom G7 CGM DIRECTLY over BLE from an Apple Watch (no phone in the loop), to closed-loop dose an Omnipod the watch borrowed. This is novel on watchOS, but the SENSOR, its BLE protocol, and the crypto are identical to what xDrip/Juggluco/Loop-G7SensorKit already implement on iOS/Android.\n\nHARD FACTS from field testing (treat as ground truth):\n- Arch…",
      "lastProgressAt": 1784608448414,
      "tokens": 115479,
      "toolCalls": 24,
      "durationMs": 434412,
      "resultPreview": "I have enough from primary source (the actual Java/C++ in the repo) plus the author's own docs and wiki. Here are the findings.\n\n---\n\n# How Juggluco connects to & reads the Dexcom G7 over BLE — including Wear OS\n\n**Repo / authorship (verified):** Juggluco is by Jaap Korthals Altes (`j-kaltes`), GPLv3, at github.com/j-kaltes/Juggluco (not jamorham — that's xDrip+). I cloned `primary` and read the a…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "xdrip-android",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "a78ade337e46b6b25",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1784608015123,
      "queuedAt": 1784608010959,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/private/tmp/claude-501/-Users-jeremybarnum-Downloads-Loop-…",
      "promptPreview": "CONTEXT — a from-stock Loop watchOS app reads a Dexcom G7 CGM DIRECTLY over BLE from an Apple Watch (no phone in the loop), to closed-loop dose an Omnipod the watch borrowed. This is novel on watchOS, but the SENSOR, its BLE protocol, and the crypto are identical to what xDrip/Juggluco/Loop-G7SensorKit already implement on iOS/Android.\n\nHARD FACTS from field testing (treat as ground truth):\n- Arch…",
      "lastProgressAt": 1784608408765,
      "tokens": 76263,
      "toolCalls": 24,
      "durationMs": 393642,
      "resultPreview": "I now have ground-truth from xDrip's actual source. Here are my findings.\n\n---\n\n# How xDrip+ handles the Dexcom G7 — evidence for the watchOS connection problem\n\n**Sources are the live master branch of `NightscoutFoundation/xDrip`.** I downloaded and grepped the two authoritative files rather than trusting doc prose (the docs contradict each other — the Intermittent page says \"scans every five min…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "g7-protocol",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "ade1ef42af62682c1",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1784608015840,
      "queuedAt": 1784608010959,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/Users/jeremybarnum/Downloads/Loop/March2026/LoopWorkspace-…",
      "promptPreview": "CONTEXT — a from-stock Loop watchOS app reads a Dexcom G7 CGM DIRECTLY over BLE from an Apple Watch (no phone in the loop), to closed-loop dose an Omnipod the watch borrowed. This is novel on watchOS, but the SENSOR, its BLE protocol, and the crypto are identical to what xDrip/Juggluco/Loop-G7SensorKit already implement on iOS/Android.\n\nHARD FACTS from field testing (treat as ground truth):\n- Arch…",
      "lastProgressAt": 1784608653891,
      "tokens": 131270,
      "toolCalls": 33,
      "durationMs": 638051,
      "resultPreview": "I have everything needed. Here is my synthesis.\n\n---\n\n# Dexcom G7 BLE Connection Model — findings\n\n## Bottom line (the central question)\n\n**After a client reads an EGV, the G7 firmware DISCONNECTS the client. It does NOT keep the client connected to push the next EGV as a notification ~5 min later.** \"Connect-per-reading\" is not a design choice your team made in isolation — it is the *only* model …"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "corebluetooth",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "aac53d007e1e786bb",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1784608018301,
      "queuedAt": 1784608010959,
      "attempt": 1,
      "lastToolName": "WebSearch",
      "lastToolSummary": "iOS bluetoothd auto connection whitelist initiator scheduli…",
      "promptPreview": "CONTEXT — a from-stock Loop watchOS app reads a Dexcom G7 CGM DIRECTLY over BLE from an Apple Watch (no phone in the loop), to closed-loop dose an Omnipod the watch borrowed. This is novel on watchOS, but the SENSOR, its BLE protocol, and the crypto are identical to what xDrip/Juggluco/Loop-G7SensorKit already implement on iOS/Android.\n\nHARD FACTS from field testing (treat as ground truth):\n- Arch…",
      "lastProgressAt": 1784608321783,
      "tokens": 56320,
      "toolCalls": 19,
      "durationMs": 303482,
      "resultPreview": "I have enough primary and secondary evidence to answer all five questions rigorously. Here are my findings.\n\n---\n\n# CoreBluetooth first principles — findings for the G7 connect-per-reading problem\n\n**Confidence legend:** [DOC] = Apple documentation or Apple-staff statement; [REP] = corroborated developer reports; [INF] = my inference from link-layer / CB semantics, flagged as such.\n\n## Bottom line…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "binary-feasibility",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "acf179c1b2d4c1dce",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1784608017071,
      "queuedAt": 1784608010959,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/tmp/g7bt.swift",
      "promptPreview": "CONTEXT — a from-stock Loop watchOS app reads a Dexcom G7 CGM DIRECTLY over BLE from an Apple Watch (no phone in the loop), to closed-loop dose an Omnipod the watch borrowed. This is novel on watchOS, but the SENSOR, its BLE protocol, and the crypto are identical to what xDrip/Juggluco/Loop-G7SensorKit already implement on iOS/Android.\n\nHARD FACTS from field testing (treat as ground truth):\n- Arch…",
      "lastProgressAt": 1784608301371,
      "tokens": 52049,
      "toolCalls": 16,
      "durationMs": 284300,
      "resultPreview": "I have what I need, including the decisive primary source: Loop's own G7 BLE manager source. Here is the assessment.\n\n---\n\n# Skeptical assessment: reverse-engineering \"shortcuts\" for the watchOS G7 connect problem\n\n## Bottom line up front\n\nBoth shortcuts are aimed at the wrong target. They would tell you **how to talk to a G7** — a problem you have already solved (your first read of every session …"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "refute-rootcause",
      "phaseIndex": 2,
      "phaseTitle": "Adversarial",
      "agentId": "a5ed932a5f7b7a553",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1784608659739,
      "queuedAt": 1784608655547,
      "attempt": 1,
      "promptPreview": "CONTEXT — a from-stock Loop watchOS app reads a Dexcom G7 CGM DIRECTLY over BLE from an Apple Watch (no phone in the loop), to closed-loop dose an Omnipod the watch borrowed. This is novel on watchOS, but the SENSOR, its BLE protocol, and the crypto are identical to what xDrip/Juggluco/Loop-G7SensorKit already implement on iOS/Android.\n\nHARD FACTS from field testing (treat as ground truth):\n- Arch…",
      "lastProgressAt": 1784608743480,
      "tokens": 61067,
      "toolCalls": 0,
      "durationMs": 83740,
      "resultPreview": "# REFUTATION: \"The CBCentralManager becomes half-poisoned; recreating it is the cure\"\n\nI was asked to break this conclusion, not defend it. Here is the strongest case that it is wrong, built entirely from the evidence in your six sources plus first-principles CoreBluetooth/link-layer semantics.\n\n## The conclusion smuggles in two claims. Both are weak.\n\nYour conclusion is really a conjunction: (1) …"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "refute-architecture",
      "phaseIndex": 2,
      "phaseTitle": "Adversarial",
      "agentId": "a9305100313f93136",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1784608659130,
      "queuedAt": 1784608655547,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/Users/jeremybarnum/Downloads/Loop/March2026/LoopWorkspace-…",
      "promptPreview": "CONTEXT — a from-stock Loop watchOS app reads a Dexcom G7 CGM DIRECTLY over BLE from an Apple Watch (no phone in the loop), to closed-loop dose an Omnipod the watch borrowed. This is novel on watchOS, but the SENSOR, its BLE protocol, and the crypto are identical to what xDrip/Juggluco/Loop-G7SensorKit already implement on iOS/Android.\n\nHARD FACTS from field testing (treat as ground truth):\n- Arch…",
      "lastProgressAt": 1784608746845,
      "tokens": 65892,
      "toolCalls": 1,
      "durationMs": 87715,
      "resultPreview": "Verified the linchpin in primary source (`G7BluetoothManager.swift:236–243`, Pete Schwamb's own comment). Here is the refutation.\n\n---\n\n# Refuting connect-per-reading — and where the refutation actually lands\n\n## The strongest case for a persistent connection (steelmanned)\n\nIf the watch could open the link once, run J-PAKE + cert + AES a single time, subscribe to the EGV/control/backfill character…"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "synthesis",
      "phaseIndex": 3,
      "phaseTitle": "Synthesize",
      "agentId": "aa5d2a0f4fa1666fd",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1784608750495,
      "queuedAt": 1784608746847,
      "attempt": 1,
      "promptPreview": "CONTEXT — a from-stock Loop watchOS app reads a Dexcom G7 CGM DIRECTLY over BLE from an Apple Watch (no phone in the loop), to closed-loop dose an Omnipod the watch borrowed. This is novel on watchOS, but the SENSOR, its BLE protocol, and the crypto are identical to what xDrip/Juggluco/Loop-G7SensorKit already implement on iOS/Android.\n\nHARD FACTS from field testing (treat as ground truth):\n- Arch…",
      "lastProgressAt": 1784608822212,
      "tokens": 69543,
      "toolCalls": 0,
      "durationMs": 71717,
      "resultPreview": "# G7-on-watchOS connection problem — honest synthesis\n\n## 1. VERDICT on \"half-poisoned CBCentralManager\": PARTLY RIGHT, WRONG WHERE IT COUNTS (the label and the fix)\n\nThe *phenomenon* you named is real and documented: a central whose scan/RX path keeps delivering ads during a window while the armed connect never completes. Apple-forum reports of stuck-`.connecting` since iOS 11, the iOS-17 state-r…"
    }
  ],
  "totalTokens": 719337,
  "totalToolCalls": 130
}